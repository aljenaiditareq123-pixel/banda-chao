# 🔧 حل مشكلة Deploy Failed - Status 127

**المشكلة:** Deploy فشل مع status 127 ❌

---

## 📋 **الخطوات:**

### **الخطوة 1: فحص Logs**

1. في Render Dashboard، اضغط على **"Logs"** في القائمة الجانبية
2. ابحث عن الأخطاء (Errors) في آخر Deploy
3. انسخ الأخطاء وأرسلها لي

---

### **الخطوة 2: التحقق من Build Command**

في Render Dashboard:

1. اضغط على **"Settings"** في القائمة الجانبية
2. ابحث عن **"Build Command"**
3. يجب أن يكون:
   ```
   npm install && npx prisma generate && npm run build
   ```

---

### **الخطوة 3: التحقق من Start Command**

في نفس صفحة Settings:

1. ابحث عن **"Start Command"**
2. يجب أن يكون:
   ```
   npm start
   ```

---

### **الخطوة 4: التحقق من Root Directory**

في نفس صفحة Settings:

1. ابحث عن **"Root Directory"**
2. يجب أن يكون:
   ```
   server
   ```

---

### **الخطوة 5: التحقق من Environment Variables**

في Render Dashboard:

1. اضغط على **"Environment"** في القائمة الجانبية
2. تحقق من وجود جميع المتغيرات:
   - ✅ `DATABASE_URL`
   - ✅ `JWT_SECRET`
   - ✅ `JWT_EXPIRES_IN`
   - ✅ `NODE_ENV`
   - ✅ `FRONTEND_URL`
   - ✅ `SEED_SECRET`

---

### **الخطوة 6: إعادة Deploy**

بعد التحقق من كل شيء:

1. اضغط على **"Manual Deploy"** في أعلى الصفحة
2. اختر **"Deploy latest commit"**
3. انتظر حتى يكتمل Deploy (5-10 دقائق)

---

## ⚠️ **أسباب شائعة لـ Status 127:**

1. **Build Command خاطئ** - تحقق من Build Command
2. **Start Command خاطئ** - تحقق من Start Command
3. **Root Directory خاطئ** - يجب أن يكون `server`
4. **Dependencies مفقودة** - تحقق من package.json
5. **Environment Variables مفقودة** - تحقق من Environment Variables

---

## 🔍 **التحقق من Logs:**

**مهم جداً:** Logs ستخبرك بالضبط ما المشكلة!

1. اضغط على **"Logs"**
2. ابحث عن آخر Deploy
3. ابحث عن كلمة **"Error"** أو **"Failed"**
4. انسخ الخطأ وأرسله لي

---

## ✅ **بعد إصلاح المشكلة:**

بعد أن يصبح Deploy ناجح:

1. ✅ تحقق من Health Check:
   ```bash
   curl https://banda-chao-backend.onrender.com/api/health
   ```

2. ✅ شغّل Seed:
   ```bash
   curl -X POST https://banda-chao-backend.onrender.com/api/v1/seed \
     -H "Content-Type: application/json" \
     -d '{"secret": "banda-chao-secret-2025"}'
   ```

---

## 🆘 **إذا استمرت المشكلة:**

1. **انسخ Logs** من Render
2. **أرسلها لي** وسأساعدك في إصلاحها

---

**📅 تاريخ:** اليوم  
**✍️ الحالة:** ⚠️ **يحتاج إصلاح**

