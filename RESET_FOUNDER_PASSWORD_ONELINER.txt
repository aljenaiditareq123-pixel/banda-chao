# 🔐 أمر واحد لتحديث كلمة مرور حساب المؤسس
# One-Line Command to Reset Founder Password

## ✅ الأمر الجاهز للنسخ واللصق:

```bash
cd src/server && node -e "const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); (async () => { try { const email = 'aljenaiditareq123@gmail.com'; const newPassword = 'Admin123'; const hash = await bcrypt.hash(newPassword, 10); await prisma.\$executeRaw\`UPDATE users SET password = \${hash}, updated_at = NOW() WHERE email = \${email}\`; console.log('✅ SUCCESS: Password updated to Admin123'); console.log('📧 Email:', email); } catch(e) { console.error('❌ ERROR:', e.message); if(e.stack) console.error('Stack:', e.stack); } finally { await prisma.\$disconnect(); } })();"
```

---

## 📋 شرح الأمر:

1. **`cd src/server`** - الانتقال إلى مجلد server
2. **`node -e "..."`** - تنفيذ كود JavaScript مباشرة
3. **استخدام `\$executeRaw`** - لتنفيذ SQL مباشرة (أكثر موثوقية من update)
4. **`bcrypt.hash('Admin123', 10)`** - تشفير كلمة المرور الجديدة
5. **`UPDATE users SET password = ...`** - تحديث كلمة المرور في قاعدة البيانات

---

## ⚠️ ملاحظات:

- تأكد أنك في مجلد `src/server` قبل التنفيذ
- الأمر يستخدم `$executeRaw` بدلاً من `update` لضمان العمل حتى مع اختلافات Schema
- كلمة المرور الجديدة: `Admin123`
- البريد الإلكتروني: `aljenaiditareq123@gmail.com`

---

**بعد التنفيذ الناجح:**
- Email: `aljenaiditareq123@gmail.com`
- Password: `Admin123`
