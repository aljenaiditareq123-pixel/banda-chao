# ✅ التحقق من DATABASE_URL في Render

**الموقع الحالي:** Render Dashboard → Environment Variables

---

## 📋 **الخطوات:**

### **الخطوة 1: تحقق من DATABASE_URL**

1. في صفحة Environment Variables
2. ابحث عن `DATABASE_URL` في القائمة
3. اضغط على أيقونة **العين** (👁️) بجانب القيمة لإظهارها
4. تحقق من أن القيمة صحيحة

---

### **الخطوة 2: إذا كان DATABASE_URL موجوداً**

إذا كان `DATABASE_URL` موجوداً بالفعل:

1. اضغط على أيقونة **العين** (👁️) لإظهار القيمة
2. تحقق من أن القيمة تحتوي على:
   - `postgresql://`
   - Project ID: `gtnyspavjsoolvnphihs`
   - `?sslmode=require` في النهاية
3. إذا كانت القيمة صحيحة، **لا حاجة لتغييرها**
4. إذا كانت القيمة خاطئة أو ناقصة، **حدّثها**

---

### **الخطوة 3: إذا كان DATABASE_URL غير موجود أو خاطئ**

1. اضغط على زر **"Edit"** (✏️) في أعلى الصفحة
2. ابحث عن `DATABASE_URL` في القائمة
3. إذا كان موجوداً:
   - اضغط على `DATABASE_URL`
   - حدّث القيمة بـ Connection String من Supabase
   - أضف `?sslmode=require` في النهاية
4. إذا لم يكن موجوداً:
   - اضغط على **"Add Environment Variable"** أو **"+**
   - **Key:** `DATABASE_URL`
   - **Value:** الصق Connection String من Supabase (مع `?sslmode=require`)
5. **احفظ** التغييرات

---

### **الخطوة 4: أعد تشغيل Backend**

بعد التحقق/تحديث `DATABASE_URL`:

1. في Render Dashboard
2. اضغط على **"Manual Deploy"** → **"Restart"**
3. أو انتظر حتى يعيد Render تشغيل الخدمة تلقائياً

---

## 🔍 **التحقق من Environment Variables المطلوبة:**

تأكد من وجود جميع المتغيرات التالية:

| المتغير | الحالة | ملاحظات |
|---------|--------|---------|
| `DATABASE_URL` | ✅ موجود | تحقق من القيمة |
| `JWT_SECRET` | ✅ موجود | تحقق من القيمة |
| `JWT_EXPIRES_IN` | ✅ موجود | يجب أن يكون `7d` |
| `NODE_ENV` | ✅ موجود | يجب أن يكون `production` |
| `FRONTEND_URL` | ✅ موجود | تحقق من القيمة |
| `SEED_SECRET` | ✅ موجود | يجب أن يكون `banda-chao-secret-2025` |

---

## 📝 **ملاحظات:**

1. **DATABASE_URL:** يجب أن يحتوي على `?sslmode=require` في النهاية
2. **القيم المخفية:** اضغط على أيقونة العين (👁️) لإظهار القيم
3. **التحديث:** اضغط على "Edit" لتحديث القيم

---

## ✅ **بعد التحقق:**

بعد التحقق من `DATABASE_URL` وإعادة التشغيل:

1. سأتحقق من Health endpoint
2. سأشغّل Prisma Migrations
3. سأشغّل Seed API

---

**📅 تاريخ:** اليوم  
**✍️ الحالة:** ✅ **جاهز - تحقق من DATABASE_URL**

