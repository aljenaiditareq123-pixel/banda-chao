# 🔍 التحقق من قيمة Environment Variables

**التاريخ:** 6 نوفمبر 2025  
**الحالة:** المتغير موجود في جميع البيئات، لكن القيمة مخفية

---

## ✅ **ما تم اكتشافه:**

- ✅ `NEXT_PUBLIC_API_URL` موجود في:
  - Development
  - Preview
  - Production
- ❓ القيمة مخفية (dots) - نحتاج التحقق منها

---

## 🔧 **الخطوات: التحقق من القيمة وتحديثها**

### **الخطوة 1: فتح القيمة**

1. في صفحة **Environment Variables**
2. ابحث عن `NEXT_PUBLIC_API_URL` في القائمة
3. اضغط على أيقونة **العين** (👁️) أو **النقاط الثلاث** (**...**) بجانب المتغير
4. اختر **"Edit"** (تعديل)

---

### **الخطوة 2: التحقق من القيمة**

عند فتح التعديل، ستظهر القيمة الحالية. تحقق من:

**✅ القيمة الصحيحة:**
```
https://banda-chao-backend.onrender.com
```

**❌ القيم الخاطئة:**
```
https://banda-chao-backend.onrender.com/  ← خطأ! (يوجد / في النهاية)
http://localhost:3001/api/v1  ← خطأ! (localhost)
```

---

### **الخطوة 3: تحديث القيمة (إذا كانت خاطئة)**

1. احذف القيمة الحالية
2. الصق: `https://banda-chao-backend.onrender.com`
3. ⚠️ **مهم جداً:** لا تضيف `/` في النهاية
4. اضغط **"Save"** (حفظ)

---

### **الخطوة 4: تحديث جميع البيئات**

يجب تحديث القيمة في:
- ✅ **Production** (الأهم!)
- ✅ **Preview**
- ✅ **Development**

**كيفية التحديث:**
1. لكل بيئة، اضغط على **Edit** (قلم)
2. تأكد من أن القيمة: `https://banda-chao-backend.onrender.com` (بدون `/`)
3. اضغط **Save**

---

### **الخطوة 5: التحقق من `NEXT_PUBLIC_SOCKET_URL`**

1. ابحث عن `NEXT_PUBLIC_SOCKET_URL` في القائمة
2. إذا كان موجوداً:
   - افتح **Edit** لكل بيئة
   - تأكد من أن القيمة: `https://banda-chao-backend.onrender.com` (بدون `/`)
   - احفظ التغييرات
3. إذا لم يكن موجوداً:
   - اضغط **"+ Add Another"**
   - Key: `NEXT_PUBLIC_SOCKET_URL`
   - Value: `https://banda-chao-backend.onrender.com` (بدون `/`)
   - اختر **"Production"** و **"Preview"** و **"Development"**
   - اضغط **Save**

---

### **الخطوة 6: Redeploy (مهم جداً!)**

بعد تحديث القيم، **يجب Redeploy**:

1. اذهب إلى تبويب **"Deployments"**
2. اضغط على النقاط الثلاث (**...**) بجانب آخر Deployment
3. اختر **"Redeploy"** (إعادة النشر)
4. اضغط **"Redeploy"** للتأكيد
5. انتظر **2-3 دقائق** حتى يكتمل النشر

⚠️ **مهم:** بدون Redeploy، التغييرات لن تظهر!

---

## ✅ **بعد Redeploy:**

1. افتح: `https://banda-chao.vercel.app`
2. اضغط **F12** لفتح Developer Tools
3. اذهب إلى تبويب **"Console"**
4. ابحث عن:
   - ✅ لا توجد أخطاء (Errors)
   - ✅ رسائل API calls ناجحة

---

## 🔍 **التحقق من Network:**

1. في Developer Tools
2. اذهب إلى تبويب **"Network"**
3. ابحث عن:
   - ✅ طلبات إلى `banda-chao-backend.onrender.com` (صحيح)
   - ❌ لا توجد طلبات إلى `localhost:3001` (خطأ)

---

## ✅ **النتيجة المتوقعة:**

بعد Redeploy:
- ✅ الموقع يعرض الفيديوهات والمنتجات
- ✅ جميع الصفحات تعمل بشكل صحيح
- ✅ لا توجد أخطاء في Console

---

## 📋 **القيم الصحيحة:**

```
NEXT_PUBLIC_API_URL = https://banda-chao-backend.onrender.com
NEXT_PUBLIC_SOCKET_URL = https://banda-chao-backend.onrender.com
```

⚠️ **مهم:** 
- لا تضيف `/` في نهاية القيم
- تأكد من تحديث جميع البيئات (Production, Preview, Development)
- **يجب Redeploy** بعد التحديث!

---

**📅 تاريخ:** اليوم  
**✍️ الحالة:** 🔍 **يحتاج التحقق من القيمة وتحديثها + Redeploy**

