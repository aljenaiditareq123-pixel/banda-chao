# 🔧 حل بديل - إذا لا يمكن حذف `server/ $`

**المشكلة:** لا يمكن حذف `server/ $` من Build Command في Render

---

## ✅ **الحل:**

إذا كان Render يضيف `server/ $` تلقائياً ولا يمكن حذفه، **هذا طبيعي!** Render يضيف هذا تلقائياً عندما يكون Root Directory مضبوط على `server`.

**المهم:** تأكد من أن Build Command يحتوي على الأوامر الصحيحة فقط.

---

## 📋 **الإعدادات الصحيحة:**

### **في نافذة "Verify Settings":**

| الإعداد | القيمة |
|---------|--------|
| **Root Directory** | `server` |
| **Build Command** | `npm install --legacy-peer-deps && npm run build` |
| **Start Command** | `npm start` |

**ملاحظة:** Render قد يضيف `server/ $` تلقائياً في الواجهة، لكن هذا لا يؤثر على التنفيذ.

---

## 🔍 **إذا استمرت المشكلة:**

### **الحل 1: التحقق من Root Directory**

1. في Render Dashboard → Settings
2. تأكد من أن **Root Directory** هو `server` (بدون `/` في النهاية)
3. **لا تستخدم:** `server/` أو `./server` أو `/server`

---

### **الحل 2: استخدام Build Command مبسط**

جرب Build Command مبسط:

**Build Command:**
```
npm install && npm run build
```

(بدون `--legacy-peer-deps` مؤقتاً لاختبار)

---

### **الحل 3: التحقق من package.json**

تأكد من أن `server/package.json` يحتوي على:

```json
{
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  }
}
```

---

### **الحل 4: التحقق من وجود dist/**

تأكد من أن `server/tsconfig.json` يحدد `outDir` بشكل صحيح:

```json
{
  "compilerOptions": {
    "outDir": "./dist"
  }
}
```

---

## 🆘 **إذا استمرت المشكلة:**

### **فحص Logs:**

1. في Render Dashboard → Logs
2. ابحث عن آخر Deploy
3. ابحث عن:
   - `Error`
   - `Failed`
   - `npm ERR`
   - `prisma`
   - `TypeScript`
4. **انسخ الخطأ الكامل** وأرسله لي

---

## 📝 **ملخص:**

1. ✅ **Root Directory:** `server` (بدون `/` في النهاية)
2. ✅ **Build Command:** `npm install --legacy-peer-deps && npm run build`
3. ✅ **Start Command:** `npm start`
4. ✅ **احفظ** التغييرات
5. ✅ **انتظر** Deploy

---

**📅 تاريخ:** اليوم  
**✍️ الحالة:** ✅ **جاهز - `server/ $` طبيعي إذا كان Render يضيفه تلقائياً**

