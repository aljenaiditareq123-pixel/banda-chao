# ✅ Settings - تحقق من الأوامر

## 📝 **أنت في صفحة Settings!**

---

## ⚠️ **الأوامر تبدو مقطوعة:**

- Build Command: `npm install && npx prisma genera...` (مقطوع)
- Start Command: `npm start ``` --- ### 4 Save...` (يبدو فيه نص إضافي)

---

## 🔧 **تحقق من الأوامر:**

### **1. Build Command:**

#### **اضغط "Edit" (✏️) بجانب Build Command:**
1. ستفتح نافذة تحرير
2. تأكد أن الأمر كامل:
   ```
   npm install && npx prisma generate && npm run build
   ```
3. إذا كان ناقصاً:
   - احذف كل شيء
   - اكتب بالضبط:
     ```
     npm install && npx prisma generate && npm run build
     ```
4. اضغط **"Save"** أو **"Update"**

---

### **2. Start Command:**

#### **اضغط "Edit" (✏️) بجانب Start Command:**
1. ستفتح نافذة تحرير
2. تأكد أن الأمر صحيح:
   ```
   npm start
   ```
3. إذا كان فيه نص إضافي (مثل ```` --- ### 4 Save...`):
   - احذف كل شيء
   - اكتب بالضبط:
     ```
     npm start
     ```
4. اضغط **"Save"** أو **"Update"**

---

### **3. Save Changes:**

#### **بعد تعديل الأوامر:**
1. في أسفل صفحة Settings
2. ابحث عن زر **"Save Changes"**
3. اضغط **"Save Changes"**

---

## ✅ **الأوامر الصحيحة:**

### **Build Command:**
```
npm install && npx prisma generate && npm run build
```

### **Start Command:**
```
npm start
```

---

## 📋 **ملخص:**

1. ✅ **Edit** Build Command → `npm install && npx prisma generate && npm run build`
2. ✅ **Edit** Start Command → `npm start`
3. ✅ **Save Changes** (أسفل الصفحة)

---

## ⚠️ **مهم:**

- **لا تكتب `cd server &&`**
- **لا تضع مسافات إضافية**
- **لا تضع نص إضافي**

---

**اضغط Edit على Build Command و Start Command الآن!** 🔧

