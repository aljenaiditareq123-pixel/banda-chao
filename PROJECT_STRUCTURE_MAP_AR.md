# خريطة مشروع Banda Chao (توثيق هيكل الملفات)

*اريخ الإنشاء**: 25 نوفمبر 2025  
**الغرض**: دليل مبسط للمؤسس لفهم هيكل المشروع وموقع الملفات المهمة

---

## 📍 نظرة عامة*ت

مشروع Banda Chao مقسم إلى جزئين رئيسيين:
- **الواجهة الأمامية (Frontend)**: ما يراه المستخدمون في المتصفح
- **الخادم (Backend)**: ما يعمل خلف الكواليس لمعالجة البيانات والطلبات

---

## 📁 مجلد app/ (صفحات الموقع)

هذا المجلد يحتوي على جميع صفحات الموقع التي يراها المستخدمون.

### الملفات الأساسية:

#### `app/layout.tsx`
- **الوصف**: الملف الرئيسي الذي يغلف جميع صفحات الموقع
- **متى تحتاجه**: عند تعديل الإعدادات العامة للموقع (مثل الألوان الأساسية، الخطوط)

#### `app/[locale]/layout.tsx`
- **الوصف**: الملف الذي يحدد شكل الصفحات لكل لغة (عربي، إنجليزي، صيني)
- **متى تحتاجه**: عند تعديل شريط التنقل (Navbar) أو إضافة عناصر مشتركة بين جميع الصفحات
- **يحتوي على**: شريط التنقل العلوي الذي يظهر في كل صفحة

#### `app/[locale]/page.tsx`
- **الوصف**: الصفحة الرئيسية للموقع (Homepage)
- **متى تحتاجه**: عند تعديل محتوى الصفحة الرئيسية أو إضافة أقسام جديدة
- **المسار**: يظهر عند زيارة `/ar` أو `/en` أو `/zh`

---

### صفحات المستخدمين:

#### `app/[locale]/login/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة تسجيل الدخول
- **متى تحتاجه**: عند تعديل شكل صفحة تسجيل الدخول أو إضافة حقول جديدة
- **المسار**: `/ar/login` أو `/en/login` أو `/zh/login`

#### `app/[locale]/signup/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة إنشاء حساب جديد
- **متى تحتاجه**: عند تعديل نموذج التسجيل
- **المسار**: `/ar/signup` أو `/en/signup` أو `/zh/signup`

---

### صفحات الصانعين (Makers):

#### `app/[locale]/maker/join/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة انضمام صانع جديد للمنصة
- **متى تحتاجه**: عند تعديل نموذج التسجيل كصانع
- **المسار**: `/ar/maker/join`

#### `app/[locale]/maker/dashboard/page.tsx` و `page-client.tsx`
- **الوصف**: لوحة تحكم الصانع (Dashboard)
- **متى تحتاجه**: عند إضافة ميزات جديدة للصانعين أو تعديل الواجهة
- **المسار**: `/ar/maker/dashboard`

---

### صفحات المحتوى:

#### `app/[locale]/products/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة قائمة المنتجات
- **متى تحتاجه**: عند تعديل طريقة عرض المنتجات أو إضافة فلاتر جديدة
- **المسار**: `/ar/products`

#### `app/[locale]/products/[id]/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة تفاصيل منتج واحد
- **متى تحتاجه**: عند تعديل شكل صفحة المنتج أو إضافة معلومات جديدة
- **المسار**: `/ar/products/123` (حيث 123 هو رقم المنتج)

#### `app/[locale]/makers/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة قائمة الصانعين
- **متى تحتاجه**: عند تعديل طريقة عرض الصانعين
- **المسار**: `/ar/makers`

#### `app/[locale]/makers/[id]/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة ملف صانع واحد
- **متى تحتاجه**: عند تعديل شكل صفحة الصانع
- **المسار**: `/ar/makers/123` (حيث 123 هو رقم الصانع)

#### `app/[locale]/videos/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة قائمة الفيديوهات
- **متى تحتاجه**: عند تعديل طريقة عرض الفيديوهات
- **المسار**: `/ar/videos`

#### `app/[locale]/videos/[id]/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة فيديو واحد
- **متى تحتاجه**: عند تعديل مشغل الفيديو أو إضافة ميزات
- **المسار**: `/ar/videos/123`

---

### صفحات أخرى:

#### `app/[locale]/about/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة "عن المنصة"
- **متى تحتاجه**: عند تحديث معلومات المنصة
- **المسار**: `/ar/about`

#### `app/[locale]/checkout/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة الدفع (Checkout)
- **متى تحتاجه**: عند تعديل عملية الدفع
- **المسار**: `/ar/checkout`

#### `app/[locale]/messages/[conversationId]/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة المحادثات والرسائل
- **متى تحتاجه**: عند تعديل واجهة المحادثات
- **المسار**: `/ar/messages/123`

---

### صفحات المؤسس (Founder):

#### `app/founder/page.tsx` و `page-client.tsx`
- **الوصف**: لوحة تحكم المؤسس الرئيسية
- **متى تحتاجه**: عند إضافة مؤشرات أداء جديدة (KPIs) أو تعديل الواجهة
- **المسار**: `/founder` (دائماً بالعربية)

#### `app/founder/assistant/page.tsx` و `page-client.tsx`
- **الوصف**: صفحة مساعد المؤسس (الباندا المستشار)
- **متى تحتاجه**: عند تعديل واجهة المحادثات مع الذكاء الاصطناعي
- **المسار**: `/founder/assistant`

---

## 🧩 مجلد components/ (المكونات)

هذا المجلد يحتوي على "قطع" قابلة لإعادة الاستخدام في صفحات مختلفة.

### `components/layout/` (مكونات التخطيط العام):

#### `components/layout/Navbar.tsx`
- **الوصف**: شريط التنقل العلوي الذي يظهر في كل صفحة
- **متى تحتاجه**: عند تعديل القائمة العلوية أو إضافة روابط جديدة
- **يحتوي على**: روابط (Makers, Products, Videos, About) + أزرار تسجيل الدخول + زر الرفع

#### `components/layout/AuthButtons.tsx`
- **الوصف**: أزرار تسجيل الدخول وإنشاء حساب وتسجيل الخروج
- **متى تحتاجه**: عند تعديل شكل الأزرار أو إضافة ميزات جديدة
- **الموقع**: يظهر في شريط التنقل العلوي

#### `components/layout/UploadButton.tsx`
- **الوصف**: زر "رفع المنتجات" في شريط التنقل
- **متى تحتاجه**: عند تعديل سلوك الزر (مثلاً: إضافة شروط جديدة)
- **السلوك**: 
  - إذا لم يكن المستخدم مسجلاً → يذهب لصفحة تسجيل الدخول
  - إذا كان مسجلاً لكنه ليس صانعاً → يذهب لصفحة الانضمام كصانع
  - إذا كان صانعاً → يذهب للوحة التحكم

---

### `components/home/` (مكونات الصفحة الرئيسية):

#### `components/home/HomePageClient.tsx`
- **الوصف**: المحتوى الرئيسي للصفحة الرئيسية (Hero section، الأقسام المميزة)
- **متى تحتاجه**: عند تعديل محتوى الصفحة الرئيسية أو إضافة أقسام جديدة

---

### `components/cards/` (كروت العرض):

#### `components/cards/ProductCard.tsx`
- **الوصف**: كرت عرض منتج واحد (يظهر في قوائم المنتجات)
- **متى تحتاجه**: عند تعديل شكل كرت المنتج

#### `components/cards/MakerCard.tsx`
- **الوصف**: كرت عرض صانع واحد (يظهر في قوائم الصانعين)
- **متى تحتاجه**: عند تعديل شكل كرت الصانع

#### `components/cards/VideoCard.tsx`
- **الوصف**: كرت عرض فيديو واحد (يظهر في قوائم الفيديوهات)
- **متى تحتاجه**: عند تعديل شكل كرت الفيديو

---

### `components/founder/` (مكونات لوحة المؤسس):

#### `components/founder/FounderConsole.tsx`
- **الوصف**: لوحة تحكم المؤسس الرئيسية (المؤشرات، الجداول، المحادثة مع الباندا)
- **متى تحتاجه**: عند إضافة مؤشرات أداء جديدة أو تعديل الواجهة

#### `components/founder/FounderChatPanel.tsx`
- **الوصف**: لوحة المحادثة مع الباندا المستشار
- **متى تحتاجه**: عند تعديل واجهة المحادثة

#### `components/founder/FounderDashboard.tsx`
- **الوصف**: لوحة معلومات إضافية للمؤسس
- **متى تحتاجه**: عند إضافة معلومات جديدة

---

### `components/common/` (مكونات مشتركة):

#### `components/common/Card.tsx`
- **الوصف**: كرت عام يمكن استخدامه في أي مكان
- **متى تحتاجه**: عند تعديل التصميم العام للكروت

#### `components/common/EmptyState.tsx`
- **الوصف**: رسالة تظهر عندما لا يوجد محتوى (مثلاً: "لا توجد منتجات")
- **متى تحتاجه**: عند تعديل رسائل "فارغ"

#### `components/common/ErrorState.tsx`
- **الوصف**: رسالة خطأ تظهر عند حدوث مشكلة
- **متى تحتاجه**: عند تعديل رسائل الخطأ

#### `components/common/LoadingState.tsx`
- **الوصف**: مؤشر التحميل (Loading spinner)
- **متى تحتاجه**: عند تعديل شكل مؤشر التحميل

#### `components/common/NotificationBell.tsx`
- **الوصف**: جرس الإشعارات في شريط التنقل
- **متى تحتاجه**: عند تعديل واجهة الإشعارات

#### `components/common/OnboardingModal.tsx`
- **الوصف**: نافذة الترحيب التي تظهر للمستخدمين الجدد
- **متى تحتاجه**: عند تعديل محتوى الترحيب

---

### `components/messaging/` (المحادثات):

#### `components/messaging/ChatBox.tsx`
- **الوصف**: صندوق المحادثة بين المستخدمين
- **متى تحتاجه**: عند تعديل واجهة المحادثات

---

## 🎣 مجلد hooks/ (الخطافات)

الخطافات (Hooks) هي دوال خاصة تساعد المكونات في الحصول على البيانات أو إدارة الحالة.

#### `hooks/useAuth.ts`
- **الوصف**: خطاف للحصول على معلومات المستخدم المسجل حالياً
- **متى تحتاجه**: عند إضافة ميزات تتطلب معرفة المستخدم الحالي

#### `hooks/useFounderKpis.ts`
- **الوصف**: خطاف لجلب مؤشرات الأداء للمؤسس (عدد الصانعين، المنتجات، الطلبات، إلخ)
- **متى تحتاجه**: عند إضافة مؤشرات أداء جديدة

#### `hooks/usePagination.ts`
- **الوصف**: خطاف لإدارة التصفح بين الصفحات (Pagination)
- **متى تحتاجه**: عند إضافة تصفح للصفحات في أي قائمة

#### `hooks/useClientFilters.ts`
- **الوصف**: خطاف لتصفية البيانات في الواجهة (مثلاً: تصفية المنتجات حسب الفئة)
- **متى تحتاجه**: عند إضافة فلاتر جديدة

---

## 📚 مجلد lib/ (المكتبات المساعدة)

هذا المجلد يحتوي على ملفات مساعدة تستخدم في جميع أنحاء المشروع.

#### `lib/api.ts`
- **الوصف**: الملف الرئيسي للاتصال بالخادم (Backend)
- **متى تحتاجه**: عند إضافة طلبات جديدة للخادم أو تعديل طريقة الاتصال
- **يحتوي على**: جميع الدوال التي تتصل بـ API (مثل: `getProducts`, `getMakers`, `login`, إلخ)

#### `lib/api-utils.ts`
- **الوصف**: دوال مساعدة لبناء روابط API بشكل صحيح
- **متى تحتاجه**: نادراً، فقط عند تغيير بنية روابط API

#### `lib/fetch-with-retry.ts`
- **الوصف**: دالة ذكية للاتصال بالخادم مع إعادة المحاولة تلقائياً عند الفشل
- **متى تحتاجه**: عند تحسين طريقة التعامل مع أخطاء الشبكة
- **الفائدة**: إذا فشل الاتصال (مثلاً: خطأ 429 من Render)، تحاول مرة أخرى تلقائياً

#### `lib/env-check.ts`
- **الوصف**: فحص متغيرات البيئة (Environment Variables) للتأكد من صحتها
- **متى تحتاجه**: عند إضافة متغيرات بيئة جديدة
- **الفائدة**: يتحقق من وجود جميع المتغيرات المطلوبة عند بدء التطبيق

#### `lib/formatCurrency.ts`
- **الوصف**: دالة لتنسيق الأسعار بالعملات المختلفة (دولار، يوان، ريال)
- **متى تحتاجه**: عند إضافة عملات جديدة

#### `lib/socket.ts`
- **الوصف**: اتصال مباشر (Real-time) للمحادثات والإشعارات
- **متى تحتاجه**: عند تعديل طريقة عمل المحادثات المباشرة

#### `lib/analytics.ts`
- **الوصف**: تتبع الأحداث والسلوكيات (مثل: عدد النقرات، المشاهدات)
- **متى تحتاجه**: عند إضافة تتبع لأحداث جديدة

---

## 🖥️ مجلد server/ (الخادم - Backend)

هذا المجلد يحتوي على الكود الذي يعمل على الخادم (Server) لمعالجة الطلبات والبيانات.

### `server/src/index.ts`
- **الوصف**: نقطة بداية الخادم (يبدأ منه تشغيل السيرفر)
- **متى تحتاجه**: عند إضافة ميزات عامة للخادم أو تعديل الإعدادات الأساسية

---

### `server/src/api/` (واجهات برمجة التطبيقات - APIs):

هذا المجلد يحتوي على جميع نقاط الاتصال (Endpoints) التي يستخدمها الموقع.

#### `server/src/api/auth.ts`
- **الوصف**: تسجيل الدخول، إنشاء حساب، التحقق من المستخدم
- **متى تحتاجه**: عند إضافة ميزات مصادقة جديدة

#### `server/src/api/products.ts`
- **الوصف**: جلب المنتجات، إنشاء منتج، تحديث منتج، حذف منتج
- **متى تحتاجه**: عند إضافة ميزات جديدة للمنتجات

#### `server/src/api/makers.ts`
- **الوصف**: جلب الصانعين، إنشاء ملف صانع، تحديث ملف صانع
- **متى تحتاجه**: عند إضافة ميزات جديدة للصانعين

#### `server/src/api/videos.ts`
- **الوصف**: جلب الفيديوهات، إنشاء فيديو، تحديث فيديو
- **متى تحتاجه**: عند إضافة ميزات جديدة للفيديوهات

#### `server/src/api/orders.ts`
- **الوصف**: إدارة الطلبات (إنشاء طلب، تحديث حالة الطلب)
- **متى تحتاجه**: عند إضافة ميزات جديدة للطلبات

#### `server/src/api/payments.ts`
- **الوصف**: معالجة المدفوعات (Stripe)
- **متى تحتاجه**: عند تعديل عملية الدفع

#### `server/src/api/founder.ts`
- **الوصف**: واجهات خاصة بالمؤسس (مؤشرات الأداء، الجلسات)
- **متى تحتاجه**: عند إضافة ميزات جديدة للوحة المؤسس

#### `server/src/api/ai.ts`
- **الوصف**: واجهات الذكاء الاصطناعي (الباندا المستشار)
- **متى تحتاجه**: عند تعديل سلوك الباندا أو إضافة ميزات AI جديدة
- **يحتوي على**: 
  - `/api/v1/ai/founder` - محادثة المؤسس مع الباندا
  - `/api/v1/ai/assistant` - مساعد عام
  - `/api/v1/ai/pricing-suggestion` - اقتراح أسعار للمنتجات
  - `/api/v1/ai/content-helper` - مساعد في كتابة المحتوى

#### `server/src/api/notifications.ts`
- **الوصف**: إدارة الإشعارات
- **متى تحتاجه**: عند إضافة أنواع إشعارات جديدة

#### `server/src/api/conversations.ts` و `messages.ts`
- **الوصف**: إدارة المحادثات والرسائل
- **متى تحتاجه**: عند إضافة ميزات جديدة للمحادثات

---

### `server/src/lib/` (مكتبات الخادم):

#### `server/src/lib/gemini.ts`
- **الوصف**: الاتصال بـ Google Gemini (الذكاء الاصطناعي)
- **متى تحتاجه**: عند تعديل طريقة استخدام AI أو تغيير النموذج
- **يستخدم**: نموذج `gemini-1.5-pro`

#### `server/src/lib/assistantProfiles.ts`
- **الوصف**: تعريفات "شخصيات" الباندا المختلفة (مستشار، مؤسس، تسعير، إلخ)
- **متى تحتاجه**: عند إضافة باندا جديد أو تعديل سلوك الباندا الحالي

#### `server/src/lib/stripe.ts`
- **الوصف**: الاتصال بـ Stripe للمدفوعات
- **متى تحتاجه**: عند تعديل إعدادات الدفع

#### `server/src/lib/cache.ts`
- **الوصف**: تخزين مؤقت للبيانات لتحسين الأداء
- **متى تحتاجه**: عند تحسين سرعة الموقع

---

### `server/src/middleware/` (البرمجيات الوسطية):

#### `server/src/middleware/auth.ts`
- **الوصف**: التحقق من تسجيل الدخول قبل السماح بالوصول لصفحات معينة
- **متى تحتاجه**: عند إضافة حماية لصفحات جديدة

#### `server/src/middleware/errorHandler.ts`
- **الوصف**: معالجة الأخطاء بشكل موحد
- **متى تحتاجه**: عند تحسين طريقة عرض الأخطاء

---

### `server/prisma/schema.prisma`
- **الوصف**: تعريف قاعدة البيانات (الجداول والعلاقات)
- **متى تحتاجه**: عند إضافة جداول جديدة أو تعديل الهيكل
- **يحتوي على**: تعريفات User, Maker, Product, Video, Order, Message, إلخ

---

## 🎨 مجلد public/ (الملفات العامة)

هذا المجلد يحتوي على الملفات التي يمكن الوصول إليها مباشرة من المتصفح.

#### `public/manifest.json`
- **الوصف**: ملف تعريف التطبيق (لإمكانية تثبيته كتطبيق)
- **متى تحتاجه**: عند تعديل معلومات التطبيق

#### `public/og-image.png`
- **الوصف**: الصورة التي تظهر عند مشاركة الموقع على وسائل التواصل
- **متى تحتاجه**: عند تغيير صورة المشاركة

#### `public/branding/colors.json`
- **الوصف**: تعريف الألوان الأساسية للمنصة
- **متى تحتاجه**: عند تغيير الألوان الأساسية

---

## 🔧 مجلد contexts/ (السياقات)

#### `contexts/LanguageContext.tsx`
- **الوصف**: إدارة اللغة الحالية (عربي، إنجليزي، صيني)
- **متى تحتاجه**: عند إضافة لغات جديدة

---

## 📝 مجلد types/ (الأنواع)

#### `types/index.ts`
- **الوصف**: تعريفات الأنواع (Types) المستخدمة في المشروع
- **متى تحتاجه**: عند إضافة أنواع بيانات جديدة

#### `types/founder.ts`
- **الوصف**: أنواع البيانات الخاصة بالمؤسس
- **متى تحتاجه**: عند إضافة بيانات جديدة للمؤسس

---

## 🧪 مجلد tests/ (الاختبارات)

هذا المجلد يحتوي على اختبارات تلقائية للتأكد من أن كل شيء يعمل بشكل صحيح.

---

## 📄 الملفات المهمة في الجذر:

#### `package.json`
- **الوصف**: قائمة المكتبات المستخدمة في المشروع
- **متى تحتاجه**: عند إضافة مكتبات جديدة

#### `next.config.js`
- **الوصف**: إعدادات Next.js
- **متى تحتاجه**: عند تعديل إعدادات الموقع

#### `tailwind.config.js`
- **الوصف**: إعدادات Tailwind CSS (الألوان، الخطوط، التصميم)
- **متى تحتاجه**: عند تغيير التصميم العام

---

## 🗺️ أين أجد الملفات المهمة كمؤسس؟

### إذا أردت تعديل شريط التنقل العلوي:
📁 `components/layout/Navbar.tsx`

### إذا أردت تعديل أزرار تسجيل الدخول/إنشاء حساب:
📁 `components/layout/AuthButtons.tsx`

### إذا أردت تعديل زر "رفع المنتجات":
📁 `components/layout/UploadButton.tsx`

### إذا أردت تعديل الصفحة الرئيسية:
📁 `app/[locale]/page.tsx` (الصفحة)  
📁 `components/home/HomePageClient.tsx` (المحتوى)

### إذا أردت تعديل لوحة تحكم المؤسس:
📁 `app/founder/page.tsx` (الصفحة)  
📁 `components/founder/FounderConsole.tsx` (المحتوى الرئيسي)

### إذا أردت تعديل الباندا المستشار (AI):
📁 `server/src/api/ai.ts` (الواجهات)  
📁 `server/src/lib/gemini.ts` (الاتصال بـ Gemini)  
📁 `server/src/lib/assistantProfiles.ts` (شخصيات الباندا)

### إذا أردت تعديل صفحة المنتجات:
📁 `app/[locale]/products/page.tsx` (قائمة المنتجات)  
📁 `app/[locale]/products/[id]/page.tsx` (تفاصيل منتج)

### إذا أردت تعديل صفحة الصانعين:
📁 `app/[locale]/makers/page.tsx` (قائمة الصانعين)  
📁 `app/[locale]/makers/[id]/page.tsx` (ملف صانع)

### إذا أردت تعديل قاعدة البيانات:
📁 `server/prisma/schema.prisma`

### إذا أردت إضافة API جديد:
📁 `server/src/api/` (أنشئ ملف جديد هنا)

---

## 💡 ملاحظات مهمة:

1. **الملفات التي تنتهي بـ `-client.tsx`**: هذه ملفات "عميل" (Client Components) يمكنها استخدام JavaScript في المتصفح. الملفات العادية `page.tsx` هي "خادم" (Server Components) تعمل على الخادم فقط.

2. **المجلد `banda-chao-clean/`**: هذا مجلد أرشيف/نسخة احتياطية. لا تقم بتعديله.

3. **عند إضافة صفحة جديدة**: أنشئ مجلد جديد في `app/[locale]/` مع ملف `page.tsx` بداخله.

4. **عند إضافة مكون جديد**: ضعه في `components/` في المجلد المناسب (layout, common, cards, إلخ).

---

## 📞 للمساعدة:

إذا كنت بحاجة لتعديل شيء معين ولكنك لا تعرف أين الملف:
1. ابحث في هذا الملف عن الكلمة المفتاحية
2. اتبع المسار المذكور
3. إذا لم تجد، راجع `PROJECT_STRUCTURE_CLEANUP.md` للمزيد من التفاصيل التقنية

---

**آخر تحديث**: 25 نوفمبر 2025


