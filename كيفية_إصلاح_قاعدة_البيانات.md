# 🔧 كيفية إصلاح قاعدة البيانات - خطوة بخطوة

## 📋 المشكلة

الموقع يُرجع خطأ 500 لأن جدول `makers` غير موجود في قاعدة البيانات.

## ✅ الحل السريع (5 دقائق)

### الخطوة 1: افتح Render Shell

1. **افتح المتصفح** واذهب إلى: https://dashboard.render.com
2. **سجّل الدخول** لحسابك
3. **اختر الخدمة**: `banda-chao-backend` (أو `banda-chao` إذا كان هذا اسم الخدمة)
4. **من القائمة الجانبية اليسرى**:
   - انظر إلى قسم **"MANAGE"**
   - اضغط على **"Shell"** (أيقونة Terminal)
5. **في الصفحة الرئيسية**:
   - سترى مربع أسود كبير (Web Shell)
   - هناك مؤشر وامض جاهز لكتابة الأوامر

---

### الخطوة 2: نفّذ الأمر

**انسخ هذا الأمر بالضبط** والصقه في المربع الأسود:

```bash
cd /opt/render/project/src/server && npx prisma migrate deploy --schema=./prisma/schema.prisma
```

**ثم اضغط Enter**

---

### الخطوة 3: انتظر حتى ينتهي

ستظهر رسائل مثل:
```
✅ Applying migration `20251115061250_init`
✅ Applying migration `20251115064930_add_user_role`
✅ Applying migration `20251115081910_add_orders_system`
✅ Applying migration `20251115082821_add_post_likes_and_follow_system`
✅ Applying migration `20251117054139_add_notifications_system`
✅ Applying migration `20251117061419_add_maker_user_relation`
✅ Migration applied successfully
```

**انتظر حتى ترى "Migration applied successfully" أو "No pending migrations"**

---

### الخطوة 4: تحقق من الإصلاح

**انسخ هذا الأمر** والصقه:

```bash
curl https://banda-chao-backend.onrender.com/api/v1/makers?limit=1
```

**اضغط Enter**

**يجب أن ترى**:
```json
{"data":[],"total":0,"pagination":{"limit":1,"total":0}}
```

**إذا رأيت هذا** = ✅ تم الإصلاح بنجاح!

**إذا رأيت خطأ 500** = ⚠️ المشكلة لا تزال موجودة (اتبع الخطوات التالية)

---

## 🔄 الحل الدائم (للمستقبل)

### تحديث Build Command في Render

1. **في Render Dashboard**:
   - Backend Service → **Settings** (الإعدادات)
   - اضغط على تبويب **"Build & Deploy"**
   - ابحث عن حقل **"Build Command"**

2. **تأكد أن Build Command يحتوي على**:
   ```bash
   npm install --include=dev && npx prisma generate --schema=./prisma/schema.prisma && npx prisma migrate deploy --schema=./prisma/schema.prisma && npm run build
   ```

3. **إذا كان مختلف**:
   - **احذف** Build Command القديم
   - **انسخ والصق** الأمر أعلاه
   - **احفظ** (Save)
   - **اضغط** "Manual Deploy" → "Deploy latest commit"

4. **بعد النشر**:
   - ✅ Migrations ستُشغّل تلقائياً في كل نشر جديد
   - ✅ لن تحتاج لتشغيلها يدوياً مرة أخرى

---

## ❓ أسئلة شائعة

### س: ماذا لو ظهر خطأ "command not found"؟
**ج**: تأكد أنك في المجلد الصحيح:
```bash
cd /opt/render/project/src/server
```
ثم حاول مرة أخرى.

### س: ماذا لو ظهر خطأ "schema.prisma not found"؟
**ج**: تأكد من المسار:
```bash
cd /opt/render/project/src/server
ls -la prisma/schema.prisma
```
يجب أن ترى الملف موجود.

### س: ماذا لو ظهر "No pending migrations"؟
**ج**: ✅ هذا جيد! يعني أن جميع migrations تم تطبيقها مسبقاً. المشكلة قد تكون في مكان آخر.

### س: كم من الوقت يستغرق الأمر؟
**ج**: عادة 5-30 ثانية فقط.

### س: هل سأفقد البيانات الموجودة؟
**ج**: ❌ لا. `prisma migrate deploy` آمن ولا يحذف البيانات الموجودة. يضيف فقط الجداول المفقودة.

---

## 🎯 ملخص

1. **افتح Render Shell** (القائمة الجانبية → Shell)
2. **نفّذ الأمر**:
   ```bash
   cd /opt/render/project/src/server && npx prisma migrate deploy --schema=./prisma/schema.prisma
   ```
3. **انتظر** حتى ينتهي
4. **تحقق**:
   ```bash
   curl https://banda-chao-backend.onrender.com/api/v1/makers?limit=1
   ```
5. **تم!** ✅

---

**إذا واجهت أي مشكلة، أرسل لقطة شاشة لرسالة الخطأ.**

