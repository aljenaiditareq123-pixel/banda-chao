# 💳 إعداد Stripe Live Mode - خطوات بسيطة
# Stripe Live Mode Setup - Simple Steps

---

## ⚠️ ملاحظة مهمة

أنت حالياً في **"Sandbox"** (وضع الاختبار). للحصول على مفاتيح الإنتاج (`sk_live_...` و `pk_live_...`)، يجب التبديل إلى **"Live mode"**.

---

## 🔄 الخطوة 1: التبديل إلى Live Mode

### في صفحة Stripe Dashboard:

1. **انظر إلى الأعلى** - ستجد بانر أزرق يقول:
   ```
   "You're testing in a sandbox..."
   ```

2. **اضغط على الزر:** `Switch to live account`
   - هذا الزر في أقصى اليمين من البانر الأزرق

3. **سيطلب منك Stripe:**
   - تأكيد التبديل
   - قد يطلب معلومات إضافية عن الشركة

4. **بعد التبديل:**
   - سيختفي البانر الأزرق
   - ستظهر "Live mode" في الأعلى بدلاً من "Sandbox"

---

## 🔑 الخطوة 2: الحصول على مفاتيح Live

### بعد التبديل إلى Live Mode:

1. **في الجانب الأيمن** - ابحث عن قسم **"API keys"**

2. **ستجد مفتاحين:**

   #### أ) Publishable key (المفتاح العام):
   - سيبدأ بـ `pk_live_...` (ليس `pk_test_...`)
   - **انسخ هذا المفتاح** - ستحتاجه لاحقاً

   #### ب) Secret key (المفتاح السري):
   - سيبدأ بـ `sk_live_...` (ليس `sk_test_...`)
   - **اضغط على "Reveal"** أو "إظهار" لرؤيته
   - **انسخ هذا المفتاح** - ستحتاجه لاحقاً

---

## 📝 الخطوة 3: تحديث المفاتيح في Render

### 3.1 تحديث في Backend:

1. **افتح هذا الرابط:**
   ```
   https://dashboard.render.com/web/srv-d44cvf3e5dus73b21io0/env
   ```

2. **ابحث عن:** `STRIPE_SECRET_KEY`
   - اضغط على القيمة
   - احذف `sk_test_...`
   - الصق `sk_live_...` (الذي نسخته من Stripe)
   - احفظ

3. **ابحث عن:** `STRIPE_PUBLISHABLE_KEY`
   - اضغط على القيمة
   - احذف `pk_test_...`
   - الصق `pk_live_...` (الذي نسخته من Stripe)
   - احفظ

4. **تحقق من:** `STRIPE_MODE`
   - يجب أن تكون القيمة: `production`
   - إذا كانت مختلفة، غيّرها إلى `production`
   - احفظ

### 3.2 تحديث في Frontend:

1. **افتح هذا الرابط:**
   ```
   https://dashboard.render.com/web/srv-d4eama15pdvs73fondkg/env
   ```

2. **ابحث عن:** `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
   - اضغط على القيمة
   - احذف `pk_test_...`
   - الصق `pk_live_...` (نفس المفتاح المستخدم في Backend)
   - احفظ

---

## ✅ الخطوة 4: التحقق

1. **انتظر 2-5 دقائق** (Render يعيد النشر تلقائياً)

2. **تحقق من Logs:**
   - Render Dashboard → Service → Logs
   - يجب ألا ترى أخطاء

3. **اختبر عملية شراء** (اختياري)

---

## 🆘 إذا لم تستطع التبديل إلى Live Mode

### الأسباب المحتملة:

1. **لم تكمل إعداد الحساب:**
   - قد يطلب Stripe معلومات إضافية
   - مثل: معلومات الشركة، IBAN، إلخ

2. **الحساب غير مفعّل:**
   - قد تحتاج إلى تفعيل الحساب أولاً

### الحل:

1. **اكمل إعداد Stripe:**
   - اذهب إلى Settings → Business settings
   - أضف معلومات الشركة
   - أضف IBAN (حساب البنك)
   - أكمل جميع الخطوات المطلوبة

2. **بعد إكمال الإعداد:**
   - حاول التبديل إلى Live mode مرة أخرى

---

## 📋 قائمة سريعة

- [ ] التبديل إلى Live Mode في Stripe
- [ ] نسخ `pk_live_...` (Publishable key)
- [ ] نسخ `sk_live_...` (Secret key)
- [ ] تحديث `STRIPE_SECRET_KEY` في Backend
- [ ] تحديث `STRIPE_PUBLISHABLE_KEY` في Backend
- [ ] تحديث `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` في Frontend
- [ ] التحقق من `STRIPE_MODE` = `production`
- [ ] انتظار إعادة النشر
- [ ] التحقق من Logs

---

## 💡 نصيحة

إذا كان Stripe يطلب معلومات إضافية (IBAN، معلومات الشركة):
- **لا تقلق** - هذا طبيعي
- **أكمل الإعداد** أولاً
- **ثم** حاول التبديل إلى Live mode

---

**ابدأ بالخطوة 1: اضغط على "Switch to live account" في Stripe! 🚀**

