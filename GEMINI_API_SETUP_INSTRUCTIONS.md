# تعليمات إعداد GEMINI_API_KEY

## المشكلة الحالية
رسالة الخطأ: "لم يتم تكوين خدمة AI يرجى التحقق من إعدادات الخادم."

هذا يعني أن `GEMINI_API_KEY` غير موجود في Environment Variables.

## الحل

### 1. الحصول على API Key من Google AI Studio

1. اذهب إلى: https://aistudio.google.com/app/api-keys
2. انقر على **"Create API key"**
3. اختر المشروع: **"Banda Chao AI"** (أو أنشئ مشروع جديد)
4. انسخ المفتاح (سيبدو مثل: `AIza...HBK0`)

### 2. إضافة المفتاح في Render Dashboard (للإنتاج)

1. اذهب إلى Render Dashboard: https://dashboard.render.com
2. اختر الخدمة: **"banda-chao-backend"** (أو اسم الخدمة الخاصة بك)
3. اذهب إلى **"Environment"** في القائمة الجانبية
4. انقر على **"Add Environment Variable"**
5. أضف:
   - **Key**: `GEMINI_API_KEY`
   - **Value**: المفتاح الذي نسخته من Google AI Studio
6. احفظ التغييرات
7. **أعد تشغيل الخدمة** (Render سيعيد التشغيل تلقائياً بعد الحفظ)

### 3. إضافة المفتاح محلياً (للتطوير)

1. افتح ملف `.env` في مجلد `server/`
   ```bash
   cd server
   nano .env  # أو استخدم محرر نصوص آخر
   ```

2. أضف السطر التالي:
   ```bash
   GEMINI_API_KEY=your-api-key-here
   ```

3. احفظ الملف

4. أعد تشغيل السيرفر:
   ```bash
   npm run dev
   ```

### 4. التحقق من الإعداد

#### أ) التحقق من Health Check:

**محلياً:**
```bash
curl http://localhost:3001/api/v1/ai/health
```

**في الإنتاج:**
```bash
curl https://banda-chao-backend.onrender.com/api/v1/ai/health
```

**النتيجة المتوقعة (إذا كان المفتاح موجود):**
```json
{
  "status": "ok",
  "service": "AI Assistant",
  "apiKeyConfigured": true,
  "apiKeyLength": 39,
  "model": "gemini-1.5-flash",
  "message": "AI service is ready",
  "timestamp": "2025-01-XX..."
}
```

**النتيجة (إذا كان المفتاح غير موجود):**
```json
{
  "status": "error",
  "service": "AI Assistant",
  "apiKeyConfigured": false,
  "apiKeyLength": 0,
  "model": "gemini-1.5-flash",
  "message": "GEMINI_API_KEY is not configured. Please set it in environment variables.",
  "timestamp": "2025-01-XX..."
}
```

#### ب) التحقق من Logs في Render:

1. اذهب إلى Render Dashboard
2. اختر الخدمة
3. افتح **"Logs"**
4. ابحث عن:
   ```
   🔍 Environment Variables Check:
   GEMINI_API_KEY: ✅ Configured
   ```

إذا رأيت `GEMINI_API_KEY: ❌ MISSING`، فالمفتاح غير موجود.

### 5. اختبار Assistant في Founder Console

بعد إضافة المفتاح وإعادة التشغيل:

1. اذهب إلى `/founder/assistant`
2. اختر "باندا المؤسس" (Founder Panda)
3. أرسل رسالة مثل: "مرحباً"
4. يجب أن تحصل على رد من الباندا

## متغيرات البيئة الإضافية (اختيارية)

### تغيير الموديل:
```bash
GEMINI_MODEL=gemini-1.5-pro  # بدلاً من gemini-1.5-flash (أبطأ لكن أقوى)
```

**الموديلات المتاحة:**
- `gemini-1.5-flash` (افتراضي - أسرع)
- `gemini-1.5-pro` (أقوى لكن أبطأ)
- `gemini-pro` (النسخة القديمة)

## الأمان

⚠️ **مهم:**
- لا تشارك `GEMINI_API_KEY` أبداً
- لا ترفع المفتاح إلى Git
- استخدم Environment Variables دائماً
- لا تطبع المفتاح الكامل في Logs (الكود الحالي يطبع فقط أول 10 أحرف في التطوير)

## استكشاف الأخطاء

### المشكلة: "401 Unauthorized"

**السبب:** المفتاح غير صحيح أو منتهي الصلاحية

**الحل:**
1. تحقق من المفتاح في Google AI Studio
2. قد تحتاج إلى إنشاء مفتاح جديد
3. تأكد من أن المفتاح لم يتم تقييده (restricted)

### المشكلة: "Rate limit exceeded"

**السبب:** تجاوزت الحد المسموح للطلبات

**الحل:**
1. انتظر قليلاً قبل المحاولة مرة أخرى
2. تحقق من Quota في Google AI Studio
3. فكر في ترقية الخطة (إذا كنت تستخدم Free tier)

### المشكلة: "AI service not configured"

**السبب:** `GEMINI_API_KEY` غير موجود

**الحل:**
1. راجع الخطوات أعلاه لإضافة المفتاح
2. تأكد من إعادة تشغيل السيرفر بعد إضافة المفتاح
3. تحقق من Health Check endpoint

## ملاحظات

- **Render:** بعد إضافة Environment Variable في Render، يتم إعادة التشغيل تلقائياً
- **Local:** يجب إعادة تشغيل السيرفر يدوياً بعد تحديث `.env`
- **Logs:** الكود يطبع تحذير عند بدء التشغيل إذا كان المفتاح مفقوداً

---

**بعد إضافة المفتاح وإعادة التشغيل، يجب أن يعمل Assistant بشكل صحيح!** ✅

