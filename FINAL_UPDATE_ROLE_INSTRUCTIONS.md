# ✅ تعليمات تحديث دور المستخدم - النهائية
## Final Update User Role Instructions

**التاريخ:** $(date)  
**الحالة:** ✅ DATABASE_URL تم العثور عليه

---

## ✅ تم العثور على DATABASE_URL

**المسار:** `postgresql://tarqahmdaljnydy@localhost:5432/banda_chao`

---

## 📋 الخطوات المطلوبة

### الخطوة 1: إنشاء حساب جديد

1. اذهب إلى: **http://localhost:3000/ar/signup**
2. املأ النموذج:
   - **الاسم:** Test User
   - **البريد الإلكتروني:** test@example.com
   - **كلمة المرور:** (أي كلمة مرور تريدها، مثلاً: password123)
3. اضغط "إنشاء حساب"
4. بعد إنشاء الحساب، ستتم إعادة توجيهك إلى الصفحة الرئيسية

---

### الخطوة 2: تحديث الدور إلى FOUNDER

بعد إنشاء الحساب، شغّل الأمر التالي في Terminal:

```bash
cd server
psql -d banda_chao -c "UPDATE users SET role = 'FOUNDER' WHERE email = 'test@example.com';"
```

**النتيجة المتوقعة:**
```
UPDATE 1
```

---

### الخطوة 3: تسجيل الخروج والدخول مرة أخرى

1. **سجّل خروج** من الموقع (إذا كنت مسجلاً)
2. **سجّل دخول** مرة أخرى بـ:
   - **البريد:** test@example.com
   - **كلمة المرور:** (الكلمة التي استخدمتها عند التسجيل)
3. **سيتم توجيهك تلقائياً** إلى: **http://localhost:3000/founder**

---

## ✅ التحقق من النجاح

بعد إتمام جميع الخطوات:
- ✅ المستخدم `test@example.com` موجود في قاعدة البيانات
- ✅ المستخدم لديه دور `FOUNDER`
- ✅ يمكن الوصول إلى لوحة تحكم المؤسس: http://localhost:3000/founder
- ✅ يمكن الوصول إلى مساعد المؤسس: http://localhost:3000/founder/assistant

---

## 🆘 استكشاف الأخطاء

### المشكلة: "UPDATE 0" (لم يتم تحديث أي سجل)

**السبب:** المستخدم غير موجود في قاعدة البيانات

**الحل:**
1. تأكد من إنشاء الحساب من: http://localhost:3000/ar/signup
2. تحقق من البريد الإلكتروني المستخدم
3. شغّل الأمر مرة أخرى

### المشكلة: "ERROR: relation 'users' does not exist"

**السبب:** اسم الجدول مختلف

**الحل:**
- استخدم: `psql -d banda_chao -c "\dt"` لرؤية جميع الجداول
- أو استخدم: `psql -d banda_chao -c "SELECT * FROM information_schema.tables WHERE table_name LIKE '%user%';"`

---

## 🎯 ملخص سريع

1. ✅ **إنشاء حساب:** http://localhost:3000/ar/signup
2. ✅ **تحديث الدور:** `psql -d banda_chao -c "UPDATE users SET role = 'FOUNDER' WHERE email = 'test@example.com';"`
3. ✅ **تسجيل الدخول:** http://localhost:3000/ar/login
4. ✅ **الوصول إلى لوحة التحكم:** http://localhost:3000/founder

---

**🎉 بعد إتمام جميع الخطوات، افتح: http://localhost:3000/founder**

