# ⚡ Cursor IDE - مرجع سريع

## 🎯 الأوامر الأساسية

### فتح الأدوات:
- **الدردشة:** `Cmd+L` (Mac) / `Ctrl+L` (Windows)
- **Composer:** `Cmd+I` (Mac) / `Ctrl+I` (Windows)
- **Terminal:** `` Ctrl+` ``

### أوامر نصية سريعة:
```
"أنشئ"        → إنشاء ملف/مكون جديد
"أضف"         → إضافة ميزة
"حسّن"        → تحسين الكود
"راجع"        → مراجعة الكود
"أصلح"        → إصلاح خطأ
"اختبر"       → طلب اختبارات
```

---

## 📝 أمثلة عملية

### إنشاء API Endpoint:
```
"أنشئ API endpoint في server/src/api/chat.ts
للحصول على قائمة المحادثات للمستخدم الحالي
يستخدم authenticateToken middleware"
```

### إضافة Validation:
```
"أضف validation لـ POST /api/v1/products
- name: مطلوب، 3-100 حرف
- price: اختياري، رقم موجب
- externalLink: مطلوب، URL صحيح"
```

### إصلاح خطأ:
```
[الصق رسالة الخطأ]

"حدث هذا الخطأ عند تشغيل npm run dev
كيف أصلحه؟"
```

---

## 🔧 المشاكل الشائعة

### Cannot find module:
```
الحل: npm install [اسم-الوحدة]
ثم: npx prisma generate (إذا كان Prisma)
```

### Port already in use:
```
الحل: غيّر PORT في .env
أو: أعد تشغيل Terminal
```

### Database connection error:
```
الحل: تحقق من DATABASE_URL في .env
```

---

## 📚 الملفات المرجعية

- `CURSOR-IDE-GUIDE.md` - الدليل الكامل
- `FULL-STACK-SETUP.md` - إعداد Backend
- `NEXT-STEPS.md` - الخطوات التالية

---

**💡 نصيحة:** استخدم الدردشة (`Cmd+L`) للأسئلة السريعة!

