# 🔧 اذهب إلى Build & Deploy Section

## ✅ **الوضع:**

- ✅ أنت في صفحة Settings ✅
- ✅ قسم "General" ظاهر

---

## 🎯 **الخطوة التالية:**

### **انتقل لأسفل في الصفحة:**

1. **قم بالتمرير لأسفل (Scroll Down)**

2. **ابحث عن قسم "Build & Deploy"**

---

## 📍 **ستجد قسم "Build & Deploy" يحتوي على:**

### **1. Root Directory:**
- حقل نصي (Text Input)
- ربما يحتوي على قيمة قديمة أو فارغ

### **2. Build Command:**
- حقل نصي (Text Input)
- ربما يحتوي على `cd server && npm install...` أو شيء آخر

### **3. Start Command:**
- حقل نصي (Text Input)
- ربما يحتوي على `cd server && npm start` أو شيء آخر

---

## 🔧 **القيم المطلوبة:**

### **1. Root Directory:**
```
server
```

### **2. Build Command:**
```
npm install && npx prisma generate && npm run build
```

### **3. Start Command:**
```
npm start
```

---

## 📝 **الخطوات:**

1. **انتقل لأسفل** حتى تجد "Build & Deploy"
2. **اضغط Edit** بجانب كل حقل (إذا كان موجوداً)
3. **انسخ القيم أعلاه**
4. **Save Changes** في أسفل الصفحة

---

**قم بالتمرير لأسفل وابحث عن "Build & Deploy"!** 🔧

