# 🔧 تحديث Environment Variables الموجودة

**التاريخ:** 6 نوفمبر 2025  
**الحالة:** المتغير موجود لكن قد تكون القيمة خاطئة

---

## ✅ **ما تم اكتشافه:**

- ✅ `NEXT_PUBLIC_API_URL` موجود بالفعل في Vercel
- ❌ لكن قد تكون القيمة خاطئة (مثل وجود `/` في النهاية)

---

## 🔧 **الحل: تحديث القيمة الحالية**

### **الخطوة 1: البحث عن المتغير الموجود**

1. في صفحة **Environment Variables**
2. ابحث عن `NEXT_PUBLIC_API_URL` في القائمة
3. ستجد زر **Edit** (قلم) بجانب المتغير

---

### **الخطوة 2: تحديث القيمة**

1. اضغط على زر **Edit** (قلم) بجانب `NEXT_PUBLIC_API_URL`
2. في حقل **"Value"** (القيمة):
   - احذف القيمة الحالية
   - الصق: `https://banda-chao-backend.onrender.com`
   - ⚠️ **مهم جداً:** لا تضيف `/` في النهاية
3. اضغط **"Save"** (حفظ)

---

### **الخطوة 3: التحقق من `NEXT_PUBLIC_SOCKET_URL`**

1. ابحث عن `NEXT_PUBLIC_SOCKET_URL` في القائمة
2. إذا كان موجوداً:
   - اضغط **Edit** (قلم)
   - تأكد من أن القيمة: `https://banda-chao-backend.onrender.com` (بدون `/`)
   - اضغط **Save**
3. إذا لم يكن موجوداً:
   - اضغط **"+ Add Another"**
   - Key: `NEXT_PUBLIC_SOCKET_URL`
   - Value: `https://banda-chao-backend.onrender.com` (بدون `/`)
   - اختر **"Production"** و **"Preview"** و **"Development"**
   - اضغط **Save**

---

### **الخطوة 4: التحقق من القيم النهائية**

بعد التحديث، يجب أن ترى:

```
✅ NEXT_PUBLIC_API_URL = https://banda-chao-backend.onrender.com
✅ NEXT_PUBLIC_SOCKET_URL = https://banda-chao-backend.onrender.com
```

⚠️ **مهم:** تأكد من عدم وجود `/` في نهاية القيم!

---

### **الخطوة 5: Redeploy**

1. اذهب إلى تبويب **"Deployments"**
2. اضغط على النقاط الثلاث (**...**) بجانب آخر Deployment
3. اختر **"Redeploy"** (إعادة النشر)
4. اضغط **"Redeploy"** للتأكيد
5. انتظر **2-3 دقائق** حتى يكتمل النشر

---

## ✅ **بعد Redeploy:**

1. افتح: `https://banda-chao.vercel.app`
2. اضغط **F12** لفتح Developer Tools
3. اذهب إلى تبويب **"Console"**
4. ابحث عن:
   - ✅ لا توجد أخطاء (Errors)
   - ✅ رسائل API calls ناجحة

---

## 🔍 **التحقق من Network:**

1. في Developer Tools
2. اذهب إلى تبويب **"Network"**
3. ابحث عن:
   - ✅ طلبات إلى `banda-chao-backend.onrender.com` (صحيح)
   - ❌ لا توجد طلبات إلى `localhost:3001` (خطأ)

---

## ✅ **النتيجة المتوقعة:**

بعد Redeploy:
- ✅ الموقع يعرض الفيديوهات والمنتجات
- ✅ جميع الصفحات تعمل بشكل صحيح
- ✅ لا توجد أخطاء في Console

---

## 📋 **القيم الصحيحة:**

```
NEXT_PUBLIC_API_URL = https://banda-chao-backend.onrender.com
NEXT_PUBLIC_SOCKET_URL = https://banda-chao-backend.onrender.com
```

⚠️ **مهم:** لا تضيف `/` في نهاية القيم!

---

**📅 تاريخ:** اليوم  
**✍️ الحالة:** 🔧 **يحتاج تحديث القيمة الحالية**

