# 🔧 دليل تحديث دور المستخدم إلى FOUNDER
## Update User Role to FOUNDER Guide

**التاريخ:** $(date)

---

## 📋 المتطلبات

1. **DATABASE_URL** يجب أن يكون موجوداً في متغيرات البيئة
2. المستخدم `test@example.com` يجب أن يكون موجوداً في قاعدة البيانات

---

## 🚀 الطريقة 1: استخدام السكريبت (الموصى بها)

### الخطوة 1: إضافة DATABASE_URL إلى ملف .env

افتح ملف `server/.env` وأضف:

```bash
DATABASE_URL="postgresql://user:password@host:port/database"
```

**ملاحظة:** استبدل القيم بقيم قاعدة البيانات الفعلية.

### الخطوة 2: تشغيل السكريبت

```bash
cd server
npx ts-node scripts/updateUserRole.ts
```

---

## 🚀 الطريقة 2: تمرير DATABASE_URL مباشرة

إذا كان `DATABASE_URL` موجوداً في متغيرات البيئة للنظام:

```bash
cd server
DATABASE_URL="postgresql://..." npx ts-node scripts/updateUserRole.ts
```

---

## 🚀 الطريقة 3: استخدام Prisma Studio (واجهة رسومية)

1. افتح Prisma Studio:
   ```bash
   cd server
   npx prisma studio
   ```

2. اذهب إلى جدول `users`
3. ابحث عن المستخدم `test@example.com`
4. انقر على السجل
5. غيّر `role` من `BUYER` إلى `FOUNDER`
6. احفظ التغييرات

---

## 🚀 الطريقة 4: استخدام SQL مباشرة

إذا كان لديك وصول مباشر إلى قاعدة البيانات:

```sql
UPDATE users
SET role = 'FOUNDER', "updatedAt" = NOW()
WHERE email = 'test@example.com';
```

---

## ✅ التحقق من النجاح

بعد تحديث الدور:

1. **سجل خروج** من الموقع (إذا كنت مسجلاً)
2. **سجل دخول** مرة أخرى بـ `test@example.com`
3. **يجب أن يتم توجيهك تلقائياً** إلى: http://localhost:3000/founder

---

## 🆘 استكشاف الأخطاء

### المشكلة: "DATABASE_URL is not set"

**الحل:**
1. أضف `DATABASE_URL` إلى `server/.env`
2. أو مرّرها كمتغير بيئة عند تشغيل السكريبت

### المشكلة: "User with email test@example.com not found"

**الحل:**
1. أنشئ حساباً جديداً بـ `test@example.com` من: http://localhost:3000/ar/signup
2. ثم شغّل السكريبت مرة أخرى

### المشكلة: "Cannot connect to database"

**الحل:**
1. تحقق من أن `DATABASE_URL` صحيح
2. تحقق من أن قاعدة البيانات تعمل
3. تحقق من إعدادات Firewall/Network

---

## 📝 ملاحظات

- السكريبت يبحث عن المستخدم `test@example.com`
- إذا كان المستخدم لديه دور `FOUNDER` بالفعل، لن يتم تحديثه
- بعد التحديث، يجب تسجيل الخروج والدخول مرة أخرى لتفعيل الدور الجديد

---

**✅ بعد تحديث الدور، افتح: http://localhost:3000/founder**

