# 🧪 دليل اختبار الدفع عبر Stripe
## Stripe Payment Testing Guide

**التاريخ:** $(date)  
**الحالة:** ✅ الخوادم قيد التشغيل

---

## 🚀 الخوادم قيد التشغيل

### ✅ Backend Server
- **المنفذ:** 5000
- **الرابط:** http://localhost:5000
- **الحالة:** ✅ قيد التشغيل

### ✅ Frontend Server
- **المنفذ:** 3000
- **الرابط:** http://localhost:3000
- **الحالة:** ✅ قيد التشغيل

---

## 🧪 خطوات اختبار الدفع

### الخطوة 1: فتح الموقع
1. افتح المتصفح
2. اذهب إلى: **http://localhost:3000**
3. اختر اللغة (عربي/إنجليزي/صيني)

---

### الخطوة 2: تسجيل الدخول
1. اضغط على "تسجيل الدخول" (Login)
2. إذا لم يكن لديك حساب:
   - اضغط على "إنشاء حساب" (Sign Up)
   - أنشئ حساب جديد
   - ثم سجل الدخول

---

### الخطوة 3: اختيار منتج
1. اذهب إلى صفحة المنتجات: **http://localhost:3000/{locale}/products**
2. اختر أي منتج
3. اضغط على "شراء" أو "Add to Cart"

---

### الخطوة 4: إتمام الطلب
1. ستنتقل إلى صفحة Checkout: **http://localhost:3000/{locale}/checkout**
2. املأ معلومات الشحن:
   - **الاسم الكامل:** أي اسم (مثل: Test User)
   - **العنوان:** أي عنوان (مثل: 123 Test Street)
   - **المدينة:** أي مدينة (مثل: Dubai)
   - **الدولة:** أي دولة (مثل: UAE)
   - **الرمز البريدي:** أي رمز (مثل: 12345)
   - **رقم الهاتف:** (اختياري)

3. راجع ملخص الطلب (السعر، الشحن، الإجمالي)

---

### الخطوة 5: الدفع عبر Stripe
1. اضغط على زر **"إتمام الدفع الآمن"** (Complete Secure Payment)
2. **ستنتقل تلقائياً** إلى صفحة Stripe Checkout

---

### الخطوة 6: استخدام بطاقة اختبار Stripe

**في صفحة Stripe Checkout، استخدم:**

#### بطاقة اختبار ناجحة:
- **رقم البطاقة:** `4242 4242 4242 4242`
- **تاريخ الانتهاء:** أي تاريخ مستقبلي (مثل: `12/25`)
- **CVV:** أي 3 أرقام (مثل: `123`)
- **الرمز البريدي:** أي 5 أرقام (مثل: `12345`)

#### بطاقات اختبار أخرى (لاختبار حالات مختلفة):

**رفض الدفع:**
- الرقم: `4000 0000 0000 0002`

**يتطلب المصادقة (3D Secure):**
- الرقم: `4000 0025 0000 3155`

**رصيد غير كاف:**
- الرقم: `4000 0000 0000 9995`

---

### الخطوة 7: التحقق من النجاح

**بعد إتمام الدفع:**
1. ستنتقل إلى صفحة النجاح: `/{locale}/checkout/success`
2. ستحصل على رسالة تأكيد
3. في Stripe Dashboard، ستظهر الدفعة:
   - اذهب إلى: https://dashboard.stripe.com/test/payments
   - يجب أن ترى الدفعة الاختبارية

---

## ✅ Checklist الاختبار

- [ ] ✅ الموقع يفتح على http://localhost:3000
- [ ] ✅ يمكن تسجيل الدخول
- [ ] ✅ يمكن عرض المنتجات
- [ ] ✅ يمكن الوصول إلى صفحة Checkout
- [ ] ✅ يمكن ملء معلومات الشحن
- [ ] ✅ عند الضغط على "إتمام الدفع"، تنتقل إلى Stripe Checkout
- [ ] ✅ يمكن إدخال بيانات بطاقة الاختبار
- [ ] ✅ الدفع يتم بنجاح
- [ ] ✅ تظهر صفحة النجاح
- [ ] ✅ الدفعة تظهر في Stripe Dashboard

---

## 🆘 استكشاف الأخطاء

### المشكلة: لا تنتقل إلى Stripe Checkout

**الحل:**
1. تحقق من Console في المتصفح (F12)
2. تحقق من أن Backend يعمل: http://localhost:5000/api/v1/health
3. تحقق من أن المفاتيح موجودة في `.env` و `.env.local`
4. أعد تشغيل الخوادم

### المشكلة: خطأ "Unauthorized"

**الحل:**
1. تأكد من تسجيل الدخول
2. تحقق من أن Token موجود في localStorage
3. سجل دخول مرة أخرى

### المشكلة: خطأ "Product not found"

**الحل:**
1. تأكد من وجود منتجات في قاعدة البيانات
2. تحقق من أن المنتج `status` هو `PUBLISHED`

---

## 📊 مراقبة الخوادم

### Backend Logs:
- راقب Terminal الذي يشغل Backend
- يجب أن ترى: `Server running on port 5000`
- عند إنشاء Checkout Session، ستظهر رسالة في Logs

### Frontend Logs:
- راقب Terminal الذي يشغل Frontend
- يجب أن ترى: `Ready on http://localhost:3000`
- أي أخطاء ستظهر في Console

---

## 🎯 النتيجة المتوقعة

بعد إتمام الدفع بنجاح:
1. ✅ يتم إنشاء Order في قاعدة البيانات
2. ✅ يتم إنشاء Stripe Checkout Session
3. ✅ يتم إرسال إشعار للحرفي (Maker)
4. ✅ يتم تحديث حالة الطلب إلى `PAID`
5. ✅ يظهر في Stripe Dashboard كدفعة ناجحة

---

**🎉 جاهز للاختبار! افتح http://localhost:3000 وابدأ!**

