# ⚡ تحديث دور المستخدم بسرعة
## Quick Update User Role

**المشكلة:** `DATABASE_URL` غير موجود في ملفات `.env`

**الحل:** يجب إضافة `DATABASE_URL` إلى `server/.env` أو تمريره مباشرة

---

## 🚀 الحل السريع

### الطريقة 1: إضافة DATABASE_URL إلى server/.env

1. افتح ملف `server/.env`
2. أضف السطر التالي (استبدل بقيم قاعدة البيانات الفعلية):
   ```bash
   DATABASE_URL="postgresql://user:password@host:port/database"
   ```
3. شغّل السكريبت:
   ```bash
   cd server
   npx ts-node scripts/updateUserRole.ts
   ```

---

### الطريقة 2: تمرير DATABASE_URL مباشرة

```bash
cd server
DATABASE_URL="postgresql://user:password@host:port/database" npx ts-node scripts/updateUserRole.ts
```

---

### الطريقة 3: الحصول على DATABASE_URL من Render

إذا كان الخادم يعمل على Render:

1. اذهب إلى Render Dashboard
2. افتح خدمة Backend
3. اذهب إلى "Environment" tab
4. انسخ قيمة `DATABASE_URL`
5. أضفها إلى `server/.env` أو مرّرها مباشرة

---

## ✅ بعد التحديث

1. **سجّل خروج** من الموقع (إذا كنت مسجلاً)
2. **سجّل دخول** مرة أخرى بـ `test@example.com`
3. **سيتم توجيهك تلقائياً** إلى: http://localhost:3000/founder

---

**ملاحظة:** إذا كان الخادم يعمل محلياً، `DATABASE_URL` موجود في مكان ما. يمكنك نسخه من Render أو من متغيرات البيئة للنظام.

