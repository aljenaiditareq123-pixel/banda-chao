# ✅ تحديث دور المستخدم - الحالة
## Update User Role - Status

**التاريخ:** $(date)

---

## 🔍 النتائج

### ✅ DATABASE_URL تم العثور عليه
- **المسار:** `postgresql://tarqahmdaljnydy@localhost:5432/banda_chao`
- **الحالة:** ✅ متصل بقاعدة البيانات

### ⚠️ المستخدم غير موجود
- **البريد المطلوب:** `test@example.com`
- **الحالة:** ❌ غير موجود في قاعدة البيانات

---

## 📋 الخطوات التالية

### الخيار 1: إنشاء حساب جديد (موصى به)

1. اذهب إلى: **http://localhost:3000/ar/signup**
2. أنشئ حساباً جديداً بـ:
   - **الاسم:** Test User
   - **البريد:** test@example.com
   - **كلمة المرور:** (أي كلمة مرور)
3. بعد إنشاء الحساب، شغّل الأمر التالي:

```bash
cd server
psql -d banda_chao -c "UPDATE users SET role = 'FOUNDER' WHERE email = 'test@example.com';"
```

---

### الخيار 2: تحديث مستخدم موجود

إذا كان لديك حساب موجود ببريد إلكتروني آخر:

```bash
cd server
psql -d banda_chao -c "UPDATE users SET role = 'FOUNDER' WHERE email = 'YOUR_EMAIL';"
```

استبدل `YOUR_EMAIL` ببريدك الإلكتروني الفعلي.

---

### الخيار 3: استخدام السكريبت بعد إنشاء الحساب

بعد إنشاء الحساب بـ `test@example.com`:

```bash
cd server
DATABASE_URL="postgresql://tarqahmdaljnydy@localhost:5432/banda_chao" npx ts-node scripts/updateUserRole.ts
```

---

## ✅ بعد التحديث

1. **سجّل خروج** من الموقع (إذا كنت مسجلاً)
2. **سجّل دخول** مرة أخرى بـ `test@example.com`
3. **سيتم توجيهك تلقائياً** إلى: **http://localhost:3000/founder**

---

## 🎯 النتيجة المتوقعة

بعد إتمام جميع الخطوات:
- ✅ المستخدم `test@example.com` لديه دور `FOUNDER`
- ✅ يمكن الوصول إلى لوحة تحكم المؤسس: http://localhost:3000/founder
- ✅ يمكن الوصول إلى مساعد المؤسس: http://localhost:3000/founder/assistant

---

**📝 ملاحظة:** يجب إنشاء الحساب أولاً قبل تحديث الدور!

