# 📘 دليل استخدام Cursor IDE مع مشروع Banda Chao Chat

## 📋 نظرة عامة

هذا الدليل يوضح كيفية استخدام **Cursor IDE** بشكل فعال لتنفيذ التطويرات المطلوبة على مشروع **Banda Chao Chat**. تم إعداد موجه أوامر شامل يحتوي على خطة تطوير متكاملة لتحويل المشروع من واجهة أمامية بسيطة إلى منصة ويب اجتماعية وتجارية متكاملة (Full-Stack).

---

## 🚀 الخطوة الأولى: فتح المشروع في Cursor IDE

### بعد فتح المشروع في Cursor:

1. **فتح Cursor IDE**
   - قم بتشغيل Cursor IDE على جهازك

2. **فتح مجلد المشروع**
   - اختر `File → Open Folder`
   - أو استخدم `Cmd+O` (Mac) / `Ctrl+O` (Windows/Linux)
   - حدد مجلد `banda-chao`

3. **الانتظار للتحميل**
   - انتظر حتى يتم تحميل المشروع بالكامل
   - تأكد من أن جميع الملفات ظاهرة في Explorer

---

## 💬 الخطوة الثانية: استخدام الموجه الشامل

### الطريقة الأولى: نسخ ولصق مباشر

1. **افتح ملف الدليل**
   - ابحث عن `FULL-STACK-SETUP.md` أو `NEXT-STEPS.md`
   - أو أي ملف توثيق يحتوي على الخطط

2. **اختر المرحلة**
   - حدد المرحلة التي تريد تنفيذها
   - انسخ محتوى المتطلبات

3. **استخدم نافذة الدردشة**
   - افتح نافذة الدردشة: `Cmd+L` (Mac) / `Ctrl+L` (Windows/Linux)
   - أو اضغط على أيقونة الدردشة في الشريط الجانبي

4. **الصق المحتوى**
   - الصق ما نسخته واضغط `Enter`
   - سيبدأ Cursor بتحليل المتطلبات وكتابة الكود

### الطريقة الثانية: استخدام Composer Mode

1. **فتح Composer**
   - استخدم `Cmd+I` (Mac) / `Ctrl+I` (Windows/Linux)
   - أو اختر `Composer` من القائمة

2. **اكتب طلبًا واضحًا**
   ```
   قم بتنفيذ المرحلة الأولى من FULL-STACK-SETUP.md:
   - إعداد البنية التحتية للخلفية
   - إنشاء مجلد server
   - إعداد Express + Prisma
   ```

3. **راقب التنفيذ**
   - سيقوم Cursor بقراءة الملفات وتنفيذ المهام تلقائيًا

---

## 📝 الخطوة الثالثة: التنفيذ المرحلي

### ⚠️ مهم جدًا: اتبع الترتيب!

#### المرحلة 1: إعداد البنية التحتية ✅
```
- إنشاء مجلد server/
- إعداد package.json
- إعداد TypeScript
- إعداد Prisma
```

#### المرحلة 2: تطوير API ✅
```
- إنشاء API Routes
- Users, Messages, Posts, Products
- ربط المسارات
```

#### المرحلة 3: المصادقة والترخيص ✅
```
- إعداد JWT
- إنشاء Auth Middleware
- حماية المسارات
```

#### المرحلة 4: التواصل اللحظي ✅
```
- إعداد WebSocket
- دمج Socket.io
- إدارة المحادثات
```

#### المرحلة 5: ربط الواجهة الأمامية ⏳
```
- إنشاء API Client
- إعداد AuthContext
- تحديث صفحات تسجيل الدخول
- تحديث صفحة الدردشة
```

#### المرحلة 6: تطوير ميزات جديدة ⏳
```
- صفحة Profile
- صفحة Feed
- تحديث صفحة Products
```

#### المرحلة 7: اللمسات الأخيرة ⏳
```
- معالجة الأخطاء
- تحسينات الأداء
- التوثيق النهائي
```

---

## 🤝 الخطوة الرابعة: التفاعل مع Cursor

### 1. طرح أسئلة توضيحية

**مثال على الأسئلة الجيدة:**
```
"ما هي أفضل طريقة لتنظيم ملفات الـ API في هذا المشروع؟"

"كيف يمكنني دمج Prisma مع Express بشكل صحيح؟"

"ما الفرق بين استخدام Supabase Auth و JWT في هذا المشروع؟"
```

### 2. طلب تعديلات أو تحسينات

**أمثلة على الطلبات:**
```
"أضف معالجة أخطاء أفضل لهذا الـ endpoint"

"قم بتحسين أداء هذه الدالة"

"أضف تعليقات توضيحية للكود بالعربية"

"أضف validation للمدخلات في هذا الـ route"
```

### 3. مراجعة الكود

**طلبات المراجعة:**
```
"راجع هذا الملف وأخبرني إذا كانت هناك مشاكل محتملة"

"هل هناك طرق أفضل لكتابة هذا الكود؟"

"تحقق من أمان هذا الـ endpoint"
```

### 4. طلب إضافات

**مثال:**
```
"أضف pagination لهذا الـ endpoint"

"أضف filter حسب التاريخ للمنتجات"

"أضف search functionality للرسائل"
```

---

## 🧪 الخطوة الخامسة: الاختبار المستمر

### 1. اختبار الخادم (Backend)

#### استخدام Terminal في Cursor:
```bash
# في terminal مدمج في Cursor
cd server
npm install
npm run dev
```

#### استخدام أدوات الاختبار:
- **Postman** - اختبار REST API
- **Thunder Client** (Extension في VS Code/Cursor)
- **curl** - من Terminal

**مثال على اختبار API:**
```bash
# Health check
curl http://localhost:3001/api/health

# Register
curl -X POST http://localhost:3001/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"123456","name":"Test User"}'
```

### 2. اختبار الواجهة الأمامية

#### تشغيل Frontend:
```bash
# في terminal جديد
npm run dev
```

#### اختبار في المتصفح:
- افتح `http://localhost:3000`
- اختبر الميزات الجديدة
- افتح Developer Tools (`F12`) لمتابعة الأخطاء

### 3. اختبار التكامل

- تأكد من أن Frontend يتواصل مع Backend
- اختبر WebSocket connections
- اختبر المصادقة والمسارات المحمية

---

## 🔧 الخطوة السادسة: حل المشاكل

### 1. عند حدوث خطأ

#### أ) نسخ رسالة الخطأ:
```
انسخ رسالة الخطأ بالكامل والصقها في الدردشة مع Cursor
```

#### ب) شرح السياق:
```
"كنت أحاول تشغيل npm run dev في مجلد server عندما ظهر هذا الخطأ"
```

#### ج) طلب الحل:
```
"هذا الخطأ يحدث عند تشغيل Prisma migration، كيف أحله؟"
```

### 2. أمثلة على المشاكل الشائعة

#### مشكلة: `Cannot find module`
```
"أخبر Cursor:"
"خطأ: Cannot find module '@prisma/client'
كيف أحل هذه المشكلة؟"

سيقترح Cursor:
- npm install @prisma/client
- npx prisma generate
```

#### مشكلة: `Port already in use`
```
"المنفذ 3001 مستخدم بالفعل، كيف أغير المنفذ؟"

سيقترح Cursor:
- تغيير PORT في .env
- أو إيقاف العملية التي تستخدم المنفذ
```

#### مشكلة: `Database connection error`
```
"فشل الاتصال بقاعدة البيانات، ما المشكلة؟"

سيقترح Cursor:
- التحقق من DATABASE_URL
- التحقق من اتصال الإنترنت
- التحقق من صحة بيانات الاتصال
```

### 3. طلب حلول بديلة

إذا لم يعمل الحل المقترح:
```
"هذا الحل لا يعمل، هل هناك طريقة أخرى؟"

أو:

"جربت هذا ولكن لا يزال الخطأ موجود، ماذا أفعل؟"
```

---

## 💡 نصائح للحصول على أفضل النتائج

### 1. كن واضحًا ومحددًا

❌ **سيء:**
```
"أضف API"
```

✅ **جيد:**
```
"أضف API endpoint لإنشاء منتج جديد في /api/v1/products
يجب أن يتطلب authentication ويقبل: name, description, imageUrl, externalLink, price, category"
```

### 2. راجع الكود المُنتج

- لا تقبل الكود بشكل أعمى
- اقرأ الكود وفهمه قبل استخدامه
- اسأل Cursor عن أي جزء غير واضح

### 3. اعمل بشكل تدريجي

- لا تحاول تنفيذ جميع المراحل دفعة واحدة
- ركز على مرحلة واحدة في كل مرة
- اختبر كل مرحلة قبل الانتقال للتالية

### 4. احفظ نسخًا احتياطية

#### استخدام Git:
```bash
# بعد إنجاز كل مرحلة
git add .
git commit -m "Completed Phase 1: Backend Infrastructure Setup"
git push
```

### 5. استفد من ميزات Cursor

#### أ) الإكمال التلقائي:
- اضغط `Tab` لقبول الاقتراحات
- اضغط `Cmd+K` (Mac) / `Ctrl+K` (Windows) لإعادة كتابة الكود

#### ب) التوثيق المدمج:
- ضع المؤشر على أي دالة للاطلاع على التوثيق
- استخدم `Cmd+Click` للانتقال للتعريف

#### ج) إعادة الهيكلة:
```
"أعد هيكلة هذا الكود لجعله أكثر تنظيماً"
```

### 6. استخدم أوامر محددة

**أمثلة على أوامر فعالة:**
```
"أنشئ TypeScript interface للمنتج يحتوي على: id, name, description, price, category"

"أضف error handling لهذا try-catch block"

"حوّل هذه الدالة إلى async/await بدلاً من promises"

"أضف JSDoc comments لهذه الدالة"
```

---

## 📚 الملفات المرجعية في المشروع

### ملفات التوثيق:
- `FULL-STACK-SETUP.md` - دليل الإعداد الكامل
- `NEXT-STEPS.md` - الخطوات التالية
- `README.md` - نظرة عامة على المشروع
- `CURSOR-IDE-GUIDE.md` - هذا الملف

### ملفات الكود المهمة:
- `server/src/index.ts` - نقطة بداية الخادم
- `server/prisma/schema.prisma` - مخطط قاعدة البيانات
- `lib/api.ts` - Frontend API Client
- `lib/socket.ts` - WebSocket Client
- `contexts/AuthContext.tsx` - Context للمصادقة

---

## 🎯 سيناريوهات عملية

### السيناريو 1: إضافة ميزة جديدة

```
1. أخبر Cursor بالهدف:
   "أريد إضافة ميزة البحث عن المنتجات"

2. اطلب التفاصيل:
   "أنشئ API endpoint للبحث في /api/v1/products/search
   يقبل query parameter ويرجع نتائج مطابقة"

3. راجع الكود:
   "راجع هذا الكود وأخبرني إذا كان هناك تحسينات"

4. اختبر:
   "كيف أختبر هذا الـ endpoint؟"
```

### السيناريو 2: إصلاح خطأ

```
1. الصق رسالة الخطأ:
   [رسالة الخطأ]

2. اشرح السياق:
   "حدث هذا الخطأ عند محاولة إنشاء منتج جديد"

3. اطلب الحل:
   "كيف أصلح هذا الخطأ؟"

4. تأكد من الحل:
   "هل هذا الحل آمن ولا يؤثر على أجزاء أخرى؟"
```

### السيناريو 3: تحسين الكود

```
1. حدد الملف/الدالة:
   "هذه الدالة في server/src/api/products.ts بطيئة"

2. اطلب التحسين:
   "حسّن الأداء بإضافة caching أو تحسين الاستعلام"

3. راجع التحسينات:
   "اشرح ما الذي تغير ولماذا هذا أفضل"
```

---

## 🔍 أوامر مفيدة في Cursor

### اختصارات لوحة المفاتيح:

| العملية | Mac | Windows/Linux |
|---------|-----|---------------|
| فتح الدردشة | `Cmd+L` | `Ctrl+L` |
| فتح Composer | `Cmd+I` | `Ctrl+I` |
| إعادة كتابة الكود | `Cmd+K` | `Ctrl+K` |
| إكمال تلقائي | `Tab` | `Tab` |
| الانتقال للتعريف | `Cmd+Click` | `Ctrl+Click` |

### أوامر نصية مفيدة:

```
"أنشئ" - لإنشاء ملفات/مكونات جديدة
"أضف" - لإضافة ميزات
"حسّن" - لتحسين الكود الموجود
"أعد كتابة" - لإعادة كتابة جزء من الكود
"راجع" - لمراجعة الكود
"اختبر" - لطلب اختبارات
"وثّق" - لطلب توثيق
```

---

## ✅ Checklist قبل البدء

- [ ] المشروع مفتوح في Cursor IDE
- [ ] جميع الملفات ظاهرة في Explorer
- [ ] Terminal جاهز للاستخدام
- [ ] قرأت ملف `FULL-STACK-SETUP.md`
- [ ] فهمت المراحل السبع
- [ ] لديك اتصال بالإنترنت (للحصول على التوثيق)
- [ ] Node.js و npm مثبتان

---

## 🎓 أمثلة عملية

### مثال 1: إنشاء صفحة جديدة

```
في Cursor، اكتب:
"أنشئ صفحة جديدة في app/chat/page.tsx
تستخدم messagesAPI لجلب الرسائل
وتستخدم WebSocket للرسائل الفورية
مع تصميم جميل ومتجاوب"
```

### مثال 2: إضافة validation

```
"أضف validation للمدخلات في API endpoint /api/v1/products
- name: مطلوب، طوله بين 3-100 حرف
- price: اختياري، يجب أن يكون رقم موجب
- externalLink: مطلوب، يجب أن يكون URL صحيح"
```

### مثال 3: تحسين الأداء

```
"حسّن استعلام Prisma في messagesAPI.getChatHistory
أضف pagination لعرض 50 رسالة في كل مرة
وأضف index على senderId و receiverId إذا لم يكن موجوداً"
```

---

## 🚨 تحذيرات مهمة

### ⚠️ لا تفعل:

1. **لا تنسخ كود بدون فهم**
   - افهم الكود قبل استخدامه
   - اسأل Cursor عن أي جزء غير واضح

2. **لا تتجاوز المراحل**
   - اتبع الترتيب المحدد
   - كل مرحلة تعتمد على السابقة

3. **لا تهمل الاختبار**
   - اختبر كل ميزة بعد إضافتها
   - تأكد من عمل كل شيء قبل الانتقال

4. **لا تنسى Git**
   - احفظ تقدمك بانتظام
   - استخدم commit messages واضحة

---

## 🎉 الخلاصة

باتباع هذا الدليل واستخدام Cursor IDE بشكل صحيح، ستتمكن من:

✅ تطوير المشروع بشكل منهجي ومنظم  
✅ فهم الكود المكتوب  
✅ حل المشاكل بسرعة  
✅ بناء تطبيق احترافي ومتكامل  

**تذكر:** Cursor IDE هو أداة قوية، لكن النجاح يعتمد على:
- الوضوح في الطلبات
- الفهم للمتطلبات
- المراجعة المستمرة
- الاختبار الشامل

---

## 📞 في حالة الحاجة للمساعدة

إذا واجهت أي مشكلة:

1. **استخدم Cursor:** اسأله مباشرة عن المشكلة
2. **راجع التوثيق:** اقرأ الملفات المرجعية
3. **اختبر خطوة بخطوة:** حدد المشكلة بدقة
4. **احفظ الأخطاء:** انسخ رسائل الخطأ بالكامل

---

**حظًا موفقًا في رحلة التطوير! 🚀**

