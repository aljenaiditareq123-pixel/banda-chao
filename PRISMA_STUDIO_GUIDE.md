# 🎨 دليل استخدام Prisma Studio لتغيير دور المستخدم
## Prisma Studio Guide - Update User Role

**التاريخ:** $(date)  
**الحالة:** ✅ Prisma Studio قيد التشغيل

---

## 🚀 Prisma Studio قيد التشغيل

**الرابط:** http://localhost:5555

إذا لم يفتح تلقائياً، افتح المتصفح واذهب إلى: **http://localhost:5555**

---

## 📝 خطوات تغيير دور المستخدم

### الخطوة 1: فتح جدول Users
1. في واجهة Prisma Studio، ستجد قائمة بجداول قاعدة البيانات على اليسار
2. **اضغط على جدول "User"** (أو "users" حسب التسمية)

---

### الخطوة 2: البحث عن المستخدم
1. بعد فتح جدول User، ستجد قائمة بجميع المستخدمين
2. **ابحث عن المستخدم** صاحب البريد الإلكتروني: **`test@example.com`**
   - يمكنك استخدام البحث في أعلى الصفحة
   - أو التمرير للعثور على المستخدم

---

### الخطوة 3: تغيير الدور
1. **اضغط مرتين (Double-click)** على حقل **"role"** في صف المستخدم `test@example.com`
2. ستظهر قائمة منسدلة (Dropdown) بالقيم المتاحة
3. **اختر "FOUNDER"** من القائمة
4. اضغط Enter أو اضغط خارج الحقل لحفظ التغيير

**قبل التغيير:**
```
role: BUYER
```

**بعد التغيير:**
```
role: FOUNDER
```

---

### الخطوة 4: حفظ التغييرات
1. بعد تغيير الدور، **ابحث عن زر "Save Changes"** أو **"Save"** في أعلى الصفحة
2. **اضغط على الزر** لحفظ التغييرات في قاعدة البيانات
3. ستظهر رسالة تأكيد بنجاح الحفظ

---

### الخطوة 5: التحقق من التغيير
1. تأكد من أن حقل `role` أصبح **"FOUNDER"**
2. تأكد من أن حقل `updatedAt` تم تحديثه تلقائياً

---

## ✅ بعد التحديث

### الخطوة 1: تسجيل الخروج
1. اذهب إلى الموقع: http://localhost:3000
2. **سجّل خروج** من الموقع (إذا كنت مسجلاً)

### الخطوة 2: تسجيل الدخول مرة أخرى
1. اذهب إلى: http://localhost:3000/ar/login
2. **سجّل دخول** بـ:
   - **البريد:** test@example.com
   - **كلمة المرور:** (كلمة المرور التي استخدمتها عند التسجيل)

### الخطوة 3: الوصول إلى لوحة تحكم المؤسس
1. بعد تسجيل الدخول، **سيتم توجيهك تلقائياً** إلى: **http://localhost:3000/founder**
2. إذا لم يتم التوجيه تلقائياً، اذهب مباشرة إلى: **http://localhost:3000/founder**

---

## 🎯 النتيجة المتوقعة

بعد إتمام جميع الخطوات:
- ✅ المستخدم `test@example.com` لديه دور `FOUNDER`
- ✅ يمكن الوصول إلى لوحة تحكم المؤسس: http://localhost:3000/founder
- ✅ يمكن الوصول إلى مساعد المؤسس: http://localhost:3000/founder/assistant

---

## 🆘 استكشاف الأخطاء

### المشكلة: لا أجد جدول "User"

**الحل:**
- تأكد من أن قاعدة البيانات متصلة
- تحقق من أن Prisma Studio يعمل بشكل صحيح
- أعد تحميل الصفحة (F5)

### المشكلة: لا أجد المستخدم test@example.com

**الحل:**
1. أنشئ حساباً جديداً من: http://localhost:3000/ar/signup
2. استخدم البريد: test@example.com
3. ثم عد إلى Prisma Studio

### المشكلة: لا يمكن حفظ التغييرات

**الحل:**
- تحقق من أن قاعدة البيانات متصلة
- تأكد من أن لديك صلاحيات الكتابة
- أعد تحميل الصفحة وحاول مرة أخرى

### المشكلة: Prisma Studio لا يفتح

**الحل:**
1. افتح المتصفح يدوياً
2. اذهب إلى: http://localhost:5555
3. إذا لم يعمل، أعد تشغيل Prisma Studio:
   ```bash
   cd server
   npx prisma studio
   ```

---

## 📸 لقطات شاشة متوقعة

### 1. واجهة Prisma Studio الرئيسية
- قائمة الجداول على اليسار
- جدول User في المنتصف
- أزرار الحفظ في الأعلى

### 2. جدول User
- قائمة بجميع المستخدمين
- أعمدة: id, email, name, role, createdAt, updatedAt
- المستخدم test@example.com في القائمة

### 3. تغيير الدور
- حقل role قابل للتعديل
- قائمة منسدلة بالقيم: FOUNDER, MAKER, BUYER
- زر Save Changes في الأعلى

---

## 🎉 جاهز للبدء!

**افتح:** http://localhost:5555

**واتبع الخطوات أعلاه لتغيير دور المستخدم إلى FOUNDER.**

---

**بعد التحديث، افتح:** http://localhost:3000/founder

