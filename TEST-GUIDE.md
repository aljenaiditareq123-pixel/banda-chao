# 🧪 دليل اختبار المشروع - Banda Chao

## ⚠️ المشكلة الحالية

**Node.js غير مثبت على النظام**

لاختبار المشروع، نحتاج تثبيت Node.js أولاً.

---

## 📥 تثبيت Node.js (macOS)

### الطريقة 1: استخدام Homebrew (الأسهل) ✅

```bash
# تثبيت Homebrew (إذا لم يكن مثبتاً)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# تثبيت Node.js
brew install node
```

### الطريقة 2: التحميل المباشر

1. اذهب إلى: https://nodejs.org/
2. حمّل النسخة LTS (Long Term Support)
3. شغّل الملف المحمّل واتبع التعليمات
4. أعد تشغيل Terminal

### الطريقة 3: استخدام nvm (Node Version Manager)

```bash
# تثبيت nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# إعادة تحميل Terminal
source ~/.zshrc

# تثبيت Node.js
nvm install --lts
nvm use --lts
```

---

## ✅ التحقق من التثبيت

بعد التثبيت، افتح Terminal جديد وتحقق:

```bash
node --version
npm --version
```

يجب أن ترى أرقام الإصدارات (مثلاً: v20.x.x و 10.x.x)

---

## 🚀 خطوات اختبار المشروع

بعد تثبيت Node.js:

### الخطوة 1: تثبيت التبعيات

```bash
cd /Users/tarqahmdaljnydy/Desktop/banda-chao
npm install
```

هذا سيأخذ بضع دقائق...

### الخطوة 2: التحقق من ملف .env.local

تأكد من وجود الملف `.env.local` ويحتوي على:
```
NEXT_PUBLIC_SUPABASE_URL=https://gtnyspavjsoolvnphihs.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_key_here
```

### الخطوة 3: تشغيل المشروع

```bash
npm run dev
```

يجب أن ترى:
```
▲ Next.js 14.2.5
- Local:        http://localhost:3000
```

### الخطوة 4: فتح المتصفح

افتح: http://localhost:3000

---

## 🧪 اختبار الميزات

### ✅ 1. الصفحة الرئيسية
- يجب أن تظهر الصفحة الرئيسية
- رسالة "欢迎来到 Banda Chao"
- أقسام الفيديوهات والمنتجات

### ✅ 2. التسجيل
- اضغط "注册"
- أنشئ حساب جديد
- يجب أن يعمل التسجيل

### ✅ 3. تسجيل الدخول
- اضغط "登录"
- سجل الدخول بالحساب الذي أنشأته

### ✅ 4. الملف الشخصي
- بعد تسجيل الدخول، اضغط على صورتك/اسمك
- يجب أن ترى صفحة الملف الشخصي
- جرّب رفع صورة شخصية

### ✅ 5. صفحات الفيديوهات
- اضغط "短视频" أو "长视频"
- يجب أن تعمل الصفحات (حتى لو كانت فارغة)

### ✅ 6. صفحة المنتجات
- اضغط "商品"
- يجب أن تعمل الصفحة

---

## 🐛 حل المشاكل الشائعة

### خطأ: "Cannot find module"
```bash
npm install
```

### خطأ: "Port 3000 already in use"
```bash
# أوقف السيرفر الآخر أو استخدم منفذ آخر
npm run dev -- -p 3001
```

### خطأ: "Invalid API key"
- تحقق من `.env.local`
- تأكد من المفاتيح صحيحة
- أعد تشغيل السيرفر

### خطأ: "relation does not exist"
- تأكد من تشغيل SQL migration في Supabase
- راجع `SETUP.md`

---

## 📝 ملاحظات الاختبار

### ما يجب أن يعمل ✅:
- ✅ فتح الصفحة الرئيسية
- ✅ التنقل بين الصفحات
- ✅ التسجيل/تسجيل الدخول
- ✅ عرض البيانات من Supabase (إذا كانت موجودة)
- ✅ الملف الشخصي
- ✅ رفع الصور الشخصية

### ما لن يعمل ❌ (لأنه غير مطوّر بعد):
- ❌ رفع الفيديوهات
- ❌ إضافة منتجات
- ❌ التعليقات
- ❌ البحث

---

## 🎯 بعد الاختبار

**أخبرني:**
1. هل المشروع يعمل؟
2. هل ظهرت أي أخطاء؟
3. ما الميزات التي تعمل؟
4. ما الذي لا يعمل؟

---

## 💡 نصيحة

إذا كنت تواجه صعوبة في تثبيت Node.js:
- استخدم الطريقة 2 (التحميل المباشر) - الأسهل
- أو اطلب المساعدة من شخص لديه خبرة تقنية


