# 🧪 دليل اختبار تسجيل الدخول اليدوي
## Manual Login Test Guide

**تاريخ:** 2025-01-04

---

## ✅ اختبار سريع (Quick Test)

### الخطوة 1: افتح الموقع
```
https://banda-chao.onrender.com/ar/login
```

### الخطوة 2: أدخل بيانات تسجيل الدخول
- **Email:** `admin@bandachao.com`
- **Password:** `password123`

### الخطوة 3: اضغط "تسجيل الدخول"

---

## ✅ ما يجب أن تراه (Expected Results)

### ✅ إذا كان كل شيء يعمل:
1. **لا يوجد خطأ** "JWT_SECRET is missing"
2. **لا يوجد خطأ** "CSRF token validation failed"
3. **يتم تسجيل الدخول بنجاح**
4. **يتم إعادة التوجيه** إلى الصفحة الرئيسية أو Dashboard

### ❌ إذا كان هناك مشكلة:

#### خطأ "JWT_SECRET is missing":
- **الحل:** تم إصلاحه - السيرفر يستخدم Fallback Value الآن
- **إذا استمر:** تحقق من Logs في Render Backend service

#### خطأ "CSRF token validation failed":
- **الحل:** تم إصلاحه - تم إضافة `trustHost: true` و `sameSite: 'lax'`
- **إذا استمر:** انتظر 2-3 دقائق بعد إعادة التشغيل

#### خطأ "Invalid email or password":
- **السبب:** المستخدم غير موجود في قاعدة البيانات
- **الحل:** قم بتشغيل `npx tsx scripts/seed-admin.ts` لإنشاء المستخدم

---

## 🔍 اختبار إضافي (Advanced Testing)

### 1. افتح Developer Console (F12)
- اضغط F12 في المتصفح
- اذهب إلى تبويب "Console"
- ابحث عن أخطاء (Errors)

### 2. افتح Network Tab
- اضغط F12 → Network
- جرب تسجيل الدخول
- ابحث عن طلب `/api/v1/auth/login`
- تحقق من:
  - Status: `200 OK` ✅
  - Response: يحتوي على `token` و `user` ✅

### 3. تحقق من Cookies
- اضغط F12 → Application → Cookies
- يجب أن ترى:
  - `auth_token` ✅
  - `next-auth.session-token` (إذا استخدمت NextAuth) ✅

---

## 📋 Checklist النهائي

- [ ] الموقع يفتح بدون أخطاء
- [ ] صفحة تسجيل الدخول تظهر بشكل صحيح
- [ ] لا يوجد خطأ "JWT_SECRET is missing"
- [ ] لا يوجد خطأ "CSRF token validation failed"
- [ ] تسجيل الدخول يعمل بنجاح
- [ ] يتم إعادة التوجيه بعد تسجيل الدخول
- [ ] يمكن الوصول إلى Dashboard/Profile

---

## 🐛 إذا استمرت المشاكل

### 1. تحقق من Render Logs:
- Backend service → Logs
- ابحث عن `[JWT_SECRET]` أو `[AUTH_MIDDLEWARE]`
- ابحث عن أخطاء

### 2. تحقق من الحالة:
- Backend service: يجب أن يكون "Live" (أخضر)
- Frontend service: يجب أن يكون "Live" (أخضر)

### 3. انتظر قليلاً:
- Render قد يكون في حالة "Sleeping"
- انتظر 30-60 ثانية بعد فتح الموقع
- جرب تسجيل الدخول مرة أخرى

---

**📅 آخر تحديث:** 2025-01-04







