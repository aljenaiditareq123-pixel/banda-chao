# 🔧 تعديل Hard-code Backend URL مؤقتاً

**التاريخ:** 6 نوفمبر 2025  
**الحالة:** تم التعديل ✅

---

## ✅ **ما تم إنجازه:**

### **التعديل في `lib/api.ts`:**

**قبل:**
```typescript
const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL 
  ? `${process.env.NEXT_PUBLIC_API_URL}/api/v1`
  : 'http://localhost:3001/api/v1';
```

**بعد:**
```typescript
// TEMPORARY: Hard-code Backend URL for testing
const API_BASE_URL = 'https://banda-chao-backend.onrender.com/api/v1';
```

---

## 📋 **الخطوات التالية:**

### **الخطوة 1: انتظر Deploy**

Vercel سيبدأ Deploy تلقائياً بعد Push. انتظر **2-3 دقائق** حتى يكتمل النشر.

---

### **الخطوة 2: افتح الموقع**

بعد اكتمال Deploy:

1. افتح: **https://banda-chao.vercel.app**
2. تحقق من:
   - ✅ الموقع يعرض الفيديوهات (بدلاً من "暂无内容")
   - ✅ الموقع يعرض المنتجات
   - ✅ جميع الصفحات تعمل بشكل صحيح

---

### **الخطوة 3: افتح Console (اختياري)**

1. اضغط **`Cmd + Option + J`** لفتح Console
2. ابحث عن:
   - `🔗 API Base URL:` - يجب أن يظهر `https://banda-chao-backend.onrender.com/api/v1`
   - أي أخطاء `❌ API Error:`

---

## ✅ **النتيجة المتوقعة:**

### **إذا ظهرت البيانات:**
- ✅ المشكلة كانت في Environment Variables
- ✅ يجب إصلاح Environment Variables في Vercel
- ✅ ثم إعادة الكود إلى استخدام Environment Variable

### **إذا لم تظهر البيانات:**
- ❌ المشكلة ليست في Environment Variables
- ❌ يجب فحص شيء آخر (CORS، Network، إلخ)

---

## 🔄 **بعد التحقق:**

### **إذا نجح Hard-code:**

1. **أصلح Environment Variables في Vercel:**
   - تأكد من أن `NEXT_PUBLIC_API_URL` = `https://banda-chao-backend.onrender.com` (بدون `/api/v1`)
   - تأكد من تطبيقها على **Production** environment

2. **أعد الكود إلى استخدام Environment Variable:**
   - أزل Hard-code
   - استخدم `process.env.NEXT_PUBLIC_API_URL` مرة أخرى

3. **Redeploy:**
   - Commit & Push التغييرات
   - انتظر Deploy

---

## ⚠️ **مهم:**

هذا تعديل **مؤقت** للاختبار فقط. يجب إعادة الكود إلى استخدام Environment Variable بعد إصلاح المشكلة.

---

**📅 تاريخ:** اليوم  
**✍️ الحالة:** ✅ **تم التعديل - في انتظار Deploy**


