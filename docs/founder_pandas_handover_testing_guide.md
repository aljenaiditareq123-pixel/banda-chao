# 🐼 دليل اختبار نظام التحويل بين الباندات (Handover System)

**التاريخ:** 2025-11-15  
**الحالة:** ✅ جاهز للاختبار

---

## 📋 نظرة عامة

تم تطوير نظام ذكي للتحويل بين الباندات الستة مع:
- **نصوص تحويل مخصصة** لكل مسار (16 مسار مختلف)
- **ملخص ذكي** من آخر رسالة مع تمييز من كتبها
- **أسئلة مقترحة** لكل باندا (3 أسئلة لكل واحد)

---

## ✅ التحقق من الكود

### 1. صحة السياق المنقول

**الكود:**
```typescript
const lastMessageObj = currentMessages.length > 0 
  ? currentMessages[currentMessages.length - 1] 
  : null;
const lastMessage = lastMessageObj?.text || null;
const lastMessageRole = lastMessageObj?.role || null;

// Role label بناءً على من كتب الرسالة
const roleLabel = lastMessageRole === 'assistant' 
  ? `رد ${sourceLabel}`
  : lastMessageRole === 'founder'
  ? 'رسالة المؤسس'
  : 'المحادثة السابقة';
```

**التحقق:**
- ✅ يتم استخراج آخر رسالة من `currentMessages[activeAssistantId]`
- ✅ يتم استخراج `role` من الرسالة
- ✅ يتم بناء `roleLabel` بشكل صحيح
- ✅ يتم تضمين السياق فقط إذا كانت الرسالة > 10 أحرف

### 2. الحوارات لا تضيع الـ activeAssistantId

**الكود:**
```typescript
// في handleHandover
setActiveAssistantId(target);  // ✅ يغير الـ active assistant أولاً
setDrafts((prev) => ({
  ...prev,
  [target]: finalMessage,  // ✅ يكتب draft للـ target فقط
}));

// في handleSubmit
setDialogs((prev) => ({
  ...prev,
  [assistantId]: [...prev[assistantId], founderMessage],  // ✅ يضيف للـ dialog الصحيح
}));

// في الـ render
const currentMessages = dialogs[activeAssistantId] ?? [];  // ✅ يأخذ من الـ active assistant
```

**التحقق:**
- ✅ `activeAssistantId` يتم تحديثه قبل تحديث الـ draft
- ✅ كل باندا له `dialogs` منفصل
- ✅ `currentMessages` يأخذ من `dialogs[activeAssistantId]` الصحيح
- ✅ رسائل المستخدم تُضاف مع `role: 'founder'`
- ✅ ردود المساعد تُضاف مع `role: 'assistant'`

---

## 🧪 سيناريوهات الاختبار

### سيناريو 1: Founder → Tech

**الخطوات:**
1. افتح `/founder/assistant/founder-brain`
2. اضغط على سؤال مقترح: "ما هي أهم 3 أولويات بعد الانتهاء من Phase 1؟"
3. أرسل السؤال
4. انتظر رد الباندا المؤسس (يجب أن يكون رد طويل > 10 أحرف)
5. اضغط: "اسأل الباندا التقني عن هذا"

**المتوقع:**
- ✅ التبويب ينتقل إلى `tech`
- ✅ حقل الإدخال يحتوي على:
  ```
  كنا نناقش قراراً استراتيجياً مع الباندا المؤسس. أريد خطة تقنية لتنفيذه:

  السياق من المحادثة السابقة (رد الباندا المؤسس):
  "أول 3 أولويات بعد Phase 1 هي: 1) نظام Notifications 2) تحسين أداء Feed..."
  ```
- ✅ النص يوضح أن هذا "رد الباندا المؤسس" (من assistant)
- ✅ المقتطف محدود بـ 200 حرف

**التحقق:**
- [ ] التبويب تغير إلى tech
- [ ] النص المخصص ظهر بشكل صحيح
- [ ] السياق من آخر رسالة ظهر
- [ ] role label صحيح ("رد الباندا المؤسس")

---

### سيناريو 2: Tech → Founder

**الخطوات:**
1. انتقل إلى `/founder/assistant/technical-brain`
2. اسأل سؤال تقني (أو استخدم سؤال مقترح)
3. انتظر رد الباندا التقني
4. اضغط: "اسأل الباندا المؤسس عن هذا"

**المتوقع:**
- ✅ التبويب ينتقل إلى `founder`
- ✅ حقل الإدخال يحتوي على:
  ```
  حددنا متطلبات تقنية. أريد قرارك الاستراتيجي حول الأولويات:

  السياق من المحادثة السابقة (رد الباندا التقني):
  "..."
  ```
- ✅ النص يوضح أن هذا "رد الباندا التقني"

**التحقق:**
- [ ] التبويب تغير إلى founder
- [ ] النص المخصص ظهر
- [ ] السياق ظهر مع role label صحيح

---

### سيناريو 3: Commerce → Content

**الخطوات:**
1. انتقل إلى `/founder/assistant/marketing-brain`
2. اسأل سؤال تجاري
3. انتظر رد باندا التجارة
4. اضغط: "اسأل باندا المحتوى عن هذا"

**المتوقع:**
- ✅ التبويب ينتقل إلى `content`
- ✅ حقل الإدخال يحتوي على:
  ```
  صممنا funnel تجاري. ساعدني بصياغة النصوص المناسبة:

  السياق من المحادثة السابقة (رد باندا التجارة):
  "..."
  ```

**التحقق:**
- [ ] النص المخصص للتحويل من commerce → content ظهر
- [ ] السياق من باندا التجارة ظهر

---

### سيناريو 4: اختبار الرسائل القصيرة

**الخطوات:**
1. في أي باندا، أرسل رسالة قصيرة: "تمام شكراً"
2. اضغط زر تحويل إلى باندا آخر

**المتوقع:**
- ✅ السياق **لا يظهر** (لأن الرسالة < 10 أحرف)
- ✅ يظهر فقط نص التحويل المخصص

**التحقق:**
- [ ] السياق لم يظهر للرسائل القصيرة
- [ ] نص التحويل ظهر فقط

---

### سيناريو 5: اختبار الحوارات المنفصلة

**الخطوات:**
1. في `founder`، أرسل رسالة: "ما هي الأولويات؟"
2. انتظر الرد
3. انتقل يدوياً (عبر tabs) إلى `tech`
4. أرسل رسالة: "كيف نصمم Notifications؟"
5. انتظر الرد
6. ارجع إلى `founder` عبر tabs

**المتوقع:**
- ✅ في `founder`: ترى محادثة founder فقط
- ✅ في `tech`: ترى محادثة tech فقط
- ✅ لا يوجد خلط بين الحوارات

**التحقق:**
- [ ] كل باندا له حوار منفصل
- [ ] لا يوجد خلط بين الحوارات

---

## 📊 نصوص التحويل المخصصة (16 مسار)

| من | إلى | النص المخصص |
|---|---|---|
| founder | tech | "كنا نناقش قراراً استراتيجياً مع الباندا المؤسس. أريد خطة تقنية لتنفيذه:" |
| founder | guard | "ناقشنا إضافة ميزة جديدة مع الباندا المؤسس. أريد تقييمك الأمني لها:" |
| founder | commerce | "حددنا أولوية استراتيجية مع الباندا المؤسس. كيف نترجمها إلى نمو تجاري:" |
| founder | content | "اتفقنا على رسالة استراتيجية مع الباندا المؤسس. ساعدني بصياغتها:" |
| founder | logistics | "قررنا خطة استراتيجية مع الباندا المؤسس. كيف ننفذها عملياً:" |
| tech | founder | "حددنا متطلبات تقنية. أريد قرارك الاستراتيجي حول الأولويات:" |
| tech | guard | "صممنا حلولاً تقنية. أريد مراجعتك الأمنية لها:" |
| guard | founder | "حددنا مخاطر أمنية. أريد قرارك الاستراتيجي حول كيفية التعامل معها:" |
| guard | tech | "اكتشفنا ثغرة أمنية. أريد حلولاً تقنية لإصلاحها:" |
| commerce | founder | "حللنا فرص تجارية. أريد قرارك الاستراتيجي حول الأولويات:" |
| commerce | content | "صممنا funnel تجاري. ساعدني بصياغة النصوص المناسبة:" |
| commerce | logistics | "خططنا حملة تجارية. كيف نضمن تدفق عملياتي سلس:" |
| content | founder | "صغنا رسائل محتوى. أريد قرارك الاستراتيجي حول الاتجاه:" |
| content | commerce | "صغنا محتوى جذاب. كيف نستخدمه لزيادة التحويل:" |
| logistics | founder | "حددنا تحديات عملياتية. أريد قرارك الاستراتيجي:" |
| logistics | commerce | "حسّنا التدفق العملياتي. كيف نستخدمه لتحسين التجربة التجارية:" |

---

## 🎯 الأسئلة المقترحة لكل باندا

### الباندا المؤسس (founder)
1. "ما هي أهم 3 أولويات بعد الانتهاء من Phase 1؟"
2. "ساعدني أرتب خارطة طريق واضحة للأشهر الثلاثة القادمة."
3. "كيف أوازن بين تطوير المزايا التقنية وزيادة عدد الحرفيين؟"

### الباندا التقني (tech)
1. "كيف يمكننا تصميم نظام Notifications متوافق مع Prisma + Express + Next.js؟"
2. "ما هي أفضل طريقة لتحسين أداء صفحة الـ feed إذا كبرت البيانات؟"
3. "كيف أنظم ملفات الـ API والـ services لتكون أسهل صيانة؟"

### الباندا الحارس (guard)
1. "ما هي أهم نقاط الضعف الأمنية المحتملة في Follow + Likes + Orders؟"
2. "اقترح عليّ خطة مبسطة لإضافة rate limiting على الـ APIs الحساسة."
3. "كيف أتأكد أن صفحات المؤسس /founder لا يمكن الوصول لها إلا مع role=FOUNDER؟"

### باندا التجارة (commerce)
1. "كيف أستفيد من صفحة /orders و /order/success لزيادة الثقة والولاء؟"
2. "ما هي 3 تحسينات بسيطة على checkout لرفع نسبة إكمال الطلب؟"
3. "اقترح عليّ أفكاراً لعروض أو باقات تناسب الحرفيين والعملاء."

### باندا المحتوى (content)
1. "اكتب لي وصفاً قصيراً جذاباً للصفحة الرئيسية بالعربية والإنجليزية."
2. "ساعدني بصياغة رسالة ترحيب لأول حرفي ينضم للمنصة."
3. "أريد 3 نماذج لوصف منتج يدوي (مثل حقيبة جلدية)."

### باندا اللوجستيات (logistics)
1. "صمم لي تدفق حالات الطلب من PENDING إلى DELIVERED مع رسالة لكل حالة."
2. "كيف أشرح للحرفيين ببساطة ماذا يحدث عندما يأتي طلب جديد؟"
3. "ما هي البيانات التي يجب أن نضيفها لاحقاً لدعم تتبع الشحن؟"

---

## 🔍 نقاط التحقق النهائية

### ✅ الكود
- [x] `handleHandover` يستخرج `lastMessageObj` بشكل صحيح
- [x] `role` يتم استخراجه من `lastMessageObj?.role`
- [x] `roleLabel` يتم بناؤه بشكل صحيح
- [x] الشرط `lastMessage.trim().length > 10` موجود
- [x] `setActiveAssistantId(target)` يتم استدعاؤه قبل تحديث الـ draft
- [x] `currentMessages` يأخذ من `dialogs[activeAssistantId]`
- [x] رسائل المستخدم تُضاف مع `role: 'founder'`
- [x] ردود المساعد تُضاف مع `role: 'assistant'`

### ✅ الوظائف
- [x] نصوص تحويل مخصصة (16 مسار)
- [x] ملخص ذكي مع role indicator
- [x] أسئلة مقترحة لكل باندا
- [x] حوارات منفصلة لكل باندا
- [x] تحويل ذكي مع سياق

---

## 🚀 جاهز للاختبار

جميع الميزات مطبقة ومتحقق منها. يمكنك الآن اختبار النظام في المتصفح.

**ملاحظة:** إذا واجهت أي مشكلة أثناء الاختبار، تأكد من:
1. أن الـ backend يعمل على `localhost:3001`
2. أن الـ frontend يعمل على `localhost:3000`
3. أنك مسجل دخول بحساب FOUNDER

---

**آخر تحديث:** 2025-11-15



