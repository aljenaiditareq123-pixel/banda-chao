# 🔍 فحص مشكلة Deploy - خطوة بخطوة

**المشكلة:** Deploy فشل ❌

---

## 📋 **الخطوات للتحقق من المشكلة:**

### **الخطوة 1: فحص Logs في Render**

1. في Render Dashboard، اضغط على **"Logs"** في القائمة الجانبية
2. ابحث عن آخر Deploy
3. ابحث عن كلمة **"Error"** أو **"Failed"** أو **"status 127"**
4. **انسخ الخطأ الكامل** وأرسله لي

---

### **الخطوة 2: التحقق من Build Command**

في Render Dashboard → Settings:

**Build Command يجب أن يكون:**
```
npm install && npx prisma generate && npm run build
```

**⚠️ تأكد من:**
- لا توجد أخطاء إملائية
- جميع الأوامر موجودة
- المسافات صحيحة

---

### **الخطوة 3: التحقق من Start Command**

في Render Dashboard → Settings:

**Start Command يجب أن يكون:**
```
npm start
```

**⚠️ تأكد من:**
- لا يوجد `ts-node` (يجب استخدام `node dist/index.js`)
- الأمر بسيط ومباشر

---

### **الخطوة 4: التحقق من Root Directory**

في Render Dashboard → Settings:

**Root Directory يجب أن يكون:**
```
server
```

**⚠️ تأكد من:**
- بدون `/` في البداية
- بدون `/` في النهاية
- فقط `server`

---

### **الخطوة 5: التحقق من Environment Variables**

في Render Dashboard → Environment:

تحقق من وجود:
- ✅ `DATABASE_URL` (مهم جداً!)
- ✅ `JWT_SECRET`
- ✅ `JWT_EXPIRES_IN` = `7d`
- ✅ `NODE_ENV` = `production`
- ✅ `FRONTEND_URL`
- ✅ `SEED_SECRET` = `banda-chao-secret-2025`

---

### **الخطوة 6: التحقق من Node Version**

في Render Dashboard → Settings:

**Node Version:**
- يجب أن يكون **18.x** أو **20.x**
- لا تستخدم **16.x** أو أقل

---

## 🔍 **مشاكل شائعة:**

### **المشكلة 1: Status 127**
**السبب:** أمر غير موجود أو لا يمكن تنفيذه  
**الحل:** تحقق من Build Command و Start Command

### **المشكلة 2: Prisma Error**
**السبب:** Prisma غير مثبت  
**الحل:** تأكد من أن `prisma` في `dependencies`

### **المشكلة 3: TypeScript Error**
**السبب:** TypeScript غير مثبت  
**الحل:** تأكد من أن `typescript` في `devDependencies`

### **المشكلة 4: Database Connection Error**
**السبب:** DATABASE_URL خاطئ  
**الحل:** تحقق من DATABASE_URL في Environment Variables

---

## 🆘 **أرسل لي:**

1. **Logs من Render** (من صفحة Logs)
2. **Build Command** (من Settings)
3. **Start Command** (من Settings)
4. **Root Directory** (من Settings)
5. **قائمة Environment Variables** (من Environment)

---

**بعد إرسال هذه المعلومات، سأقوم بإصلاح المشكلة!** 🔧

---

**📅 تاريخ:** اليوم  
**✍️ الحالة:** ⚠️ **يحتاج فحص**


