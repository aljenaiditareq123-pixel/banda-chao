# 🔑 دليل بسيط: كيفية الحصول على مفاتيح Stripe

**اللغة:** عربي  
**المستوى:** مبتدئ  
**الوقت:** 15-20 دقيقة

---

## 📍 الخطوة 1: الذهاب إلى Stripe Dashboard

### أين تذهب بالضبط:

1. **افتح المتصفح** (Chrome, Safari, Firefox... أي متصفح)

2. **اكتب في شريط العنوان:**
   ```
   https://dashboard.stripe.com
   ```
   أو ابحث في Google عن: "Stripe Dashboard"

3. **سجل الدخول:**
   - إذا كان لديك حساب: أدخل البريد الإلكتروني وكلمة المرور
   - إذا لم يكن لديك حساب: اضغط "Sign up" وأنشئ حساب جديد

---

## 🔑 الخطوة 2: الحصول على المفاتيح (Keys)

### ما هي المفاتيح التي تحتاجها؟

**مفتاحان فقط:**

1. **Publishable Key** (المفتاح العام)
   - يبدأ بـ: `pk_live_...` أو `pk_test_...`
   - هذا المفتاح آمن للمشاركة (يستخدم في Frontend)

2. **Secret Key** (المفتاح السري)
   - يبدأ بـ: `sk_live_...` أو `sk_test_...`
   - ⚠️ **مهم جداً:** هذا المفتاح سري - لا تشاركه مع أحد!

---

## 📋 الخطوة 3: كيفية الحصول على المفاتيح (خطوة بخطوة)

### الطريقة:

1. **بعد تسجيل الدخول، اذهب إلى:**
   - من القائمة الجانبية (على اليسار)
   - اضغط على: **"Developers"** (المطورون)
   - ثم اضغط على: **"API keys"** (مفاتيح API)

2. **ستجد في الصفحة:**
   - **"Publishable key"** (المفتاح العام)
   - **"Secret key"** (المفتاح السري) - قد يكون مخفياً، اضغط "Reveal" لإظهاره

3. **انسخ المفاتيح:**
   - انسخ `Publishable key` (المفتاح الذي يبدأ بـ `pk_...`)
   - انسخ `Secret key` (المفتاح الذي يبدأ بـ `sk_...`)

4. **احفظ المفاتيح:**
   - انسخهما في ملف نصي على جهازك
   - أو احفظهما في تطبيق Notes
   - ⚠️ **مهم:** لا ترفع هذا الملف إلى الإنترنت أبداً!

---

## 🎯 الخطوة 4: التحقق من نوع المفاتيح

### هل المفاتيح للإنتاج (Production) أم للاختبار (Test)?

**في أعلى صفحة Stripe Dashboard، ستجد مفتاح تبديل:**

```
[Test mode] ←→ [Live mode]
```

- **إذا كان على "Test mode":**
  - المفاتيح تبدأ بـ: `pk_test_...` و `sk_test_...`
  - هذه للاختبار فقط - لا تستقبل أموال حقيقية

- **إذا كان على "Live mode":**
  - المفاتيح تبدأ بـ: `pk_live_...` و `sk_live_...`
  - هذه للإنتاج - تستقبل أموال حقيقية ✅

**للمشروع الحقيقي، نحتاج "Live mode"!**

---

## 📝 الخطوة 5: ما الذي ستفعله بعد الحصول على المفاتيح؟

### دورك فقط:
✅ **إحضار المفاتيح** - هذا كل شيء!

### دوري (Cursor):
- ✅ إضافة المفاتيح إلى Render (Backend)
- ✅ إضافة المفاتيح إلى Vercel (Frontend)
- ✅ التأكد من أن كل شيء يعمل

**أنت لا تحتاج لفعل أي شيء تقني - فقط أحضر المفاتيح!**

---

## 🎯 ملخص سريع

### ما تحتاج فعله:

1. ✅ اذهب إلى: https://dashboard.stripe.com
2. ✅ سجل الدخول
3. ✅ اذهب إلى: Developers → API keys
4. ✅ تأكد أنك في "Live mode" (ليس Test mode)
5. ✅ انسخ `Publishable key` (يبدأ بـ `pk_live_...`)
6. ✅ انسخ `Secret key` (يبدأ بـ `sk_live_...`)
7. ✅ احفظ المفاتيح في مكان آمن
8. ✅ أرسل المفاتيح لي (Cursor) وسأقوم بإضافتها

---

## ⚠️ تحذيرات مهمة

1. **لا تشارك Secret Key مع أحد**
   - هذا المفتاح يمكنه الوصول لحسابك
   - فقط أنا (Cursor) سأستخدمه في الكود

2. **تأكد من "Live mode"**
   - نحتاج المفاتيح التي تبدأ بـ `pk_live_...` و `sk_live_...`
   - ليس `pk_test_...` و `sk_test_...`

3. **احفظ المفاتيح في مكان آمن**
   - على جهازك فقط
   - لا ترفعها إلى الإنترنت

---

## 📞 إذا واجهت مشكلة

### المشكلة: لا أجد "Developers" في القائمة
**الحل:** 
- تأكد أنك سجلت الدخول
- القائمة الجانبية على اليسار
- قد تكون تحت "More" أو "Settings"

### المشكلة: المفاتيح تبدأ بـ `pk_test_...`
**الحل:**
- في أعلى الصفحة، اضغط على "Test mode"
- سيتم التبديل إلى "Live mode"
- ثم انسخ المفاتيح الجديدة

### المشكلة: لا أستطيع رؤية Secret Key
**الحل:**
- بجانب Secret Key، اضغط على "Reveal" أو "Show"
- سيظهر المفتاح

---

## ✅ بعد إحضار المفاتيح

**أرسل لي المفاتيح بهذا الشكل:**

```
Publishable Key: pk_live_xxxxxxxxxxxxxxxxxxxxx
Secret Key: sk_live_xxxxxxxxxxxxxxxxxxxxx
```

**وسأقوم بـ:**
1. ✅ إضافتها إلى Render (Backend)
2. ✅ إضافتها إلى Vercel (Frontend)
3. ✅ إعادة نشر الخدمات
4. ✅ التأكد من أن كل شيء يعمل

---

**جاهز للبدء؟ اذهب إلى https://dashboard.stripe.com الآن! 🚀**

