# 📖 دليل استخدام Cursor IDE - Banda Chao Chat

## 🎯 نظرة عامة

هذا الدليل يوضح لك كيفية استخدام **Cursor IDE** بشكل فعال لتطوير مشروع **Banda Chao Chat** باستخدام الموجه الشامل (`cursor-prompt.md`).

---

## 🚀 البداية السريعة

### الخطوة 1: فتح المشروع
1. افتح **Cursor IDE**
2. اختر `File → Open Folder`
3. حدد مجلد `banda-chao`
4. انتظر حتى يتم تحميل المشروع

### الخطوة 2: فتح الموجه
1. في Explorer، ابحث عن ملف `cursor-prompt.md`
2. اضغط عليه لفتحه
3. راجع المراحل المتاحة (7 مراحل)

### الخطوة 3: اختيار المرحلة
- ابدأ بالمرحلة الأولى إذا كنت مبتدئًا
- أو اختر المرحلة المطلوبة حسب حالتك الحالية

---

## 💬 طريقتان لاستخدام الموجه

### الطريقة الأولى: نسخ مباشر (Recommended) ✨

#### الخطوات:

1. **افتح ملف `cursor-prompt.md`**
   ```
   - اضغط على الملف في Explorer
   - أو استخدم Cmd+P (Mac) / Ctrl+P (Windows)
   - ابحث عن "cursor-prompt.md"
   ```

2. **حدد المرحلة المطلوبة**
   ```
   مثال: المرحلة الخامسة: ربط الواجهة الأمامية
   ```

3. **انسخ المحتوى**
   - حدد النص من بداية المرحلة حتى نهايتها
   - انسخ باستخدام `Cmd+C` (Mac) / `Ctrl+C` (Windows)
   - أو استخدم `Cmd+A` لنسخ كل محتوى المرحلة

4. **افتح نافذة الدردشة**
   - اضغط `Cmd+L` (Mac) أو `Ctrl+L` (Windows)
   - أو اضغط على أيقونة الدردشة في الشريط الجانبي

5. **الصق المحتوى**
   - الصق باستخدام `Cmd+V` (Mac) / `Ctrl+V` (Windows)
   - اضغط `Enter` لإرسال الطلب

6. **راقب العمل**
   - سيقوم Cursor بتحليل المتطلبات
   - سيبدأ بكتابة الكود المطلوب
   - سينشئ الملفات والمجلدات حسب الحاجة

#### مثال عملي:

```
1. افتح cursor-prompt.md
2. ابحث عن "# 🔗 المرحلة الخامسة"
3. انسخ من العنوان حتى نهاية الخطوات
4. Cmd+L → الصق → Enter
5. انتظر حتى ينتهي Cursor من العمل
```

---

### الطريقة الثانية: Composer Mode 🎨

#### متى تستخدمها:
- عندما تريد تنفيذ مهام معقدة متعددة الخطوات
- عندما تريد Cursor أن يقرأ الملف تلقائيًا
- عندما تريد دمج عدة أوامر معًا

#### الخطوات:

1. **افتح Composer Mode**
   - اضغط `Cmd+I` (Mac) أو `Ctrl+I` (Windows)
   - أو اختر `Composer` من القائمة

2. **اكتب طلبًا واضحًا**
   ```
   مثال:
   "قم بتنفيذ المرحلة الخامسة من ملف cursor-prompt.md:
   - ربط الواجهة الأمامية بالخلفية
   - تحديث صفحات تسجيل الدخول
   - إنشاء صفحة الدردشة مع WebSocket"
   ```

3. **أو اترك Cursor يقرأ الملف**
   ```
   "اقرأ ملف cursor-prompt.md ونفذ المرحلة الخامسة بالكامل"
   ```

4. **راقب التنفيذ**
   - سيقوم Cursor بقراءة الملف
   - سيحلل المتطلبات
   - سينفذ المهام خطوة بخطوة

---

## 🎯 نصائح للحصول على أفضل النتائج

### 1. كن واضحًا ومحددًا ✅

#### ❌ طلب سيء:
```
"أضف API"
```

#### ✅ طلب جيد:
```
"أنشئ API endpoint في server/src/api/chat.ts
للحصول على قائمة المحادثات للمستخدم الحالي
يستخدم authenticateToken middleware
ويرجع قائمة المحادثات مع آخر رسالة لكل محادثة"
```

### 2. اطلب التفسير عند الحاجة 💡

إذا لم تفهم شيئًا في الكود:
```
"اشرح لي كيف تعمل هذه الدالة"
```

أو:
```
"ما الذي يحدث في هذا الجزء من الكود؟"
```

### 3. اطلب التحسينات 🔧

بعد أن ينتهي Cursor من كتابة الكود:
```
"حسّن هذا الكود بإضافة error handling"
```

أو:
```
"أضف validation للمدخلات في هذا endpoint"
```

### 4. اطلب المراجعة 🔍

قبل استخدام الكود:
```
"راجع هذا الكود وأخبرني إذا كانت هناك مشاكل محتملة"
```

### 5. اسأل عن البدائل 🤔

إذا لم يعجبك الحل:
```
"هل هناك طريقة أخرى لتنفيذ هذا؟"
```

---

## 🧪 الاختبار المستمر

### بعد كل مرحلة، اختبر:

#### 1. اختبار Backend:
```bash
# في terminal داخل Cursor
cd server
npm run dev

# افتح terminal آخر واختبر
curl http://localhost:3001/api/health
```

#### 2. اختبار Frontend:
```bash
# في terminal
npm run dev

# افتح المتصفح
open http://localhost:3000
```

#### 3. طلب اختبارات من Cursor:
```
"أنشئ test cases لهذا API endpoint"
```

أو:
```
"كيف أختبر هذه الميزة؟"
```

---

## 🔧 حل المشاكل

### عند حدوث خطأ:

#### 1. نسخ رسالة الخطأ كاملة
```
انسخ كل رسالة الخطأ من Terminal أو Console
```

#### 2. الصقها في Cursor
```
[الصق رسالة الخطأ هنا]

"حدث هذا الخطأ عند محاولة تشغيل npm run dev
كيف أصلحه؟"
```

#### 3. اشرح السياق
```
"كنت أحاول تنفيذ المرحلة الخامسة عندما ظهر هذا الخطأ"
```

### أمثلة على المشاكل الشائعة:

#### المشكلة: Cannot find module
```
الخطأ: Error: Cannot find module '@prisma/client'

الحل في Cursor:
"خطأ: Cannot find module '@prisma/client'
كيف أحل هذه المشكلة؟"

سيقترح Cursor:
- npm install @prisma/client
- npx prisma generate
```

#### المشكلة: Port already in use
```
الخطأ: Port 3001 is already in use

الحل:
"المنفذ 3001 مستخدم، كيف أغير المنفذ؟"
```

#### المشكلة: Database connection error
```
الخطأ: Can't reach database server

الحل:
"فشل الاتصال بقاعدة البيانات، ما المشكلة؟
DATABASE_URL في .env هو: [الصق الرابط]"
```

---

## 📋 سير العمل المقترح

### للمبتدئين:

1. **اقرأ المرحلة أولاً**
   - افتح `cursor-prompt.md`
   - اقرأ المرحلة التي ستقوم بتنفيذها
   - تأكد من فهمك للمتطلبات

2. **انسخ المرحلة**
   - حدد كل محتوى المرحلة
   - انسخه

3. **الصق في Cursor**
   - `Cmd+L` → الصق → `Enter`

4. **راقب التنفيذ**
   - انتظر حتى ينتهي Cursor
   - راجع الملفات المُنشأة

5. **اختبر**
   - اختبر كل ميزة تم إضافتها
   - تأكد من عمل كل شيء

6. **احفظ التقدم**
   ```bash
   git add .
   git commit -m "Completed Phase X: [اسم المرحلة]"
   ```

### للمحترفين:

1. **استخدم Composer Mode**
   ```
   Cmd+I → "نفذ المرحلة X من cursor-prompt.md"
   ```

2. **اطلب تحسينات فورية**
   ```
   "أضف error handling و logging لهذا endpoint"
   ```

3. **اطلب مراجعة**
   ```
   "راجع الكود وأخبرني بالتحسينات المحتملة"
   ```

---

## 🎓 أمثلة عملية

### مثال 1: تنفيذ مرحلة كاملة

```
1. افتح cursor-prompt.md
2. انسخ "المرحلة الخامسة" كاملة
3. Cmd+L → الصق → Enter
4. انتظر حتى ينتهي Cursor
5. اختبر:
   - npm run dev في server
   - npm run dev في المشروع الرئيسي
   - افتح http://localhost:3000
```

### مثال 2: طلب ميزة محددة

```
في Cursor (Cmd+L):
"أنشئ صفحة جديدة في app/profile/page.tsx
تستخدم usersAPI.getMe() لجلب بيانات المستخدم
وتعرض:
- الاسم والصور الشخصية
- قائمة بالمنشورات
- قائمة بالمنتجات
مع تصميم جميل ومتجاوب"
```

### مثال 3: إصلاح خطأ

```
في Terminal ظهر:
"Error: Cannot read property 'map' of undefined"

في Cursor (Cmd+L):
[الصق رسالة الخطأ]

"حدث هذا الخطأ في app/products/page.tsx
عند محاولة عرض قائمة المنتجات
كيف أصلحه؟"
```

### مثال 4: طلب تحسين

```
بعد أن ينتهي Cursor من كتابة كود:
"حسّن هذا الكود:
- أضف loading states
- أضف error handling
- أضف validation للمدخلات
- حسّن الأداء"
```

---

## 🔑 اختصارات مفيدة

| العملية | Mac | Windows/Linux |
|---------|-----|---------------|
| فتح الدردشة | `Cmd+L` | `Ctrl+L` |
| فتح Composer | `Cmd+I` | `Ctrl+I` |
| فتح Terminal | `` Ctrl+` `` | `` Ctrl+` `` |
| البحث في الملفات | `Cmd+P` | `Ctrl+P` |
| البحث في الكود | `Cmd+Shift+F` | `Ctrl+Shift+F` |
| إعادة كتابة الكود | `Cmd+K` | `Ctrl+K` |
| الانتقال للتعريف | `Cmd+Click` | `Ctrl+Click` |

---

## 📝 أوامر نصية مفيدة

### لإنشاء:
- `"أنشئ"` - لإنشاء ملف/مكون جديد
- `"أضف"` - لإضافة ميزة
- `"أنشئ API endpoint"` - لإنشاء endpoint جديد

### للتحسين:
- `"حسّن"` - لتحسين الكود
- `"أضف error handling"` - لإضافة معالجة أخطاء
- `"أضف validation"` - لإضافة التحقق

### للمراجعة:
- `"راجع"` - لمراجعة الكود
- `"اختبر"` - لطلب اختبارات
- `"اشرح"` - لشرح الكود

### للإصلاح:
- `"أصلح"` - لإصلاح خطأ
- `"حل المشكلة"` - لحل مشكلة محددة

---

## ✅ Checklist قبل البدء

قبل أن تبدأ في استخدام الموجه:

- [ ] المشروع مفتوح في Cursor IDE
- [ ] ملف `cursor-prompt.md` موجود ومقروء
- [ ] فهمت المراحل السبع
- [ ] Terminal جاهز للاستخدام
- [ ] Node.js و npm مثبتان
- [ ] قاعدة البيانات جاهزة (PostgreSQL)
- [ ] قرأت هذا الدليل بالكامل

---

## 🎯 نصائح متقدمة

### 1. استخدم Context Effectively
```
"في ملف server/src/api/messages.ts
أضف endpoint جديد للحصول على المحادثات غير المقروءة"
```

### 2. اطلب التعديلات التدريجية
```
"أولاً: أضف validation
ثانياً: أضف error handling
ثالثاً: أضف logging"
```

### 3. اطلب التوثيق
```
"أضف JSDoc comments لهذه الدالة
بالعربية والإنجليزية"
```

### 4. اطلب الاختبارات
```
"أنشئ unit tests لهذا API endpoint
باستخدام Jest"
```

### 5. اطلب التحسينات
```
"حسّن أداء هذا الاستعلام في Prisma
بإضافة indexes أو تحسين الاستعلام"
```

---

## 🚨 أخطاء شائعة يجب تجنبها

### ❌ لا تفعل:

1. **لا تنسخ كود بدون فهم**
   - افهم الكود قبل استخدامه
   - اسأل Cursor عن أي جزء غير واضح

2. **لا تتجاوز المراحل**
   - اتبع الترتيب في `cursor-prompt.md`
   - كل مرحلة تعتمد على السابقة

3. **لا تهمل الاختبار**
   - اختبر كل ميزة بعد إضافتها
   - تأكد من عمل كل شيء قبل الانتقال

4. **لا تنسى Git**
   - احفظ تقدمك بانتظام
   - استخدم commit messages واضحة

5. **لا تطلب أشياء غامضة**
   - كن محددًا وواضحًا
   - قدم أمثلة إذا أمكن

---

## 🎉 الخلاصة

باتباع هذا الدليل، ستتمكن من:

✅ استخدام Cursor IDE بشكل فعال  
✅ تطبيق الموجه الشامل بنجاح  
✅ الحصول على نتائج أفضل وأسرع  
✅ حل المشاكل بسهولة  
✅ بناء تطبيق احترافي  

**تذكر:** Cursor IDE هو أداة قوية، لكن النجاح يعتمد على:
- الوضوح في الطلبات
- الفهم للمتطلبات
- المراجعة المستمرة
- الاختبار الشامل

---

## 📞 في حالة الحاجة للمساعدة

### استخدم Cursor:
```
"ساعدني في [وصف المشكلة]"
```

### راجع الملفات:
- `cursor-prompt.md` - الموجه الشامل
- `FULL-STACK-SETUP.md` - الدليل التقني
- `PROMPT-STATUS.md` - حالة التنفيذ

### اسأل أسئلة محددة:
```
"كيف أربط Frontend مع Backend في المرحلة الخامسة؟"
```

---

**🚀 حظًا موفقًا في رحلة التطوير!**


