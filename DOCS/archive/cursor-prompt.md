# 🎯 موجه أوامر شامل لمشروع Banda Chao Chat

## 📋 نظرة عامة على المشروع

**الهدف:** تحويل تطبيق "Banda Chao Chat" من واجهة أمامية ثابتة إلى منصة ويب اجتماعية وتجارية متكاملة (Full-Stack).

**النتيجة المتوقعة:** تطبيق قوي وقابل للتطوير يدمج بين ميزات التواصل الاجتماعي والتجارة الإلكترونية، مع التركيز على تجربة المستخدم والأداء ودعم متعدد اللغات.

---

## 📝 تعليمات الاستخدام

**استخدم هذا الموجه كخريطة طريق لتنفيذ المهام خطوة بخطوة.** 

قم بتحليل كل خطوة واكتب الكود اللازم، وقم بإنشاء الملفات والمجلدات حسب الحاجة.

---

# 🚀 المرحلة الأولى: إعداد البنية التحتية للخلفية (Backend)

## الهدف:
بناء أساس قوي للخادم وقاعدة البيانات باستخدام Node.js, Express, و Prisma.

## الخطوات:

### 1. إنشاء مجلد الخادم:
- في جذر المشروع، قم بإنشاء مجلد جديد باسم `server`.

### 2. إعداد مشروع Node.js:
- انتقل إلى مجلد `server` وقم بتهيئة مشروع Node.js جديد باستخدام `npm init` أو `pnpm init`.
- قم بتثبيت الاعتماديات الأساسية: `express`, `cors`, `dotenv`, `morgan`.
- قم بتثبيت اعتماديات التطوير: `@types/express`, `@types/node`, `typescript`, `ts-node`, `nodemon`.

### 3. إعداد خادم Express بسيط:
- أنشئ ملف `src/index.ts` داخل مجلد `server`.
- اكتب كود خادم Express أساسي يستمع على منفذ (مثل `PORT=3001`) ويحتوي على نقطة نهاية تجريبية `/api/health`.

### 4. إعداد Prisma وقاعدة البيانات:
- قم بتثبيت Prisma CLI كاعتمادية تطوير: `npm add -D prisma`.
- قم بتهيئة Prisma في المشروع: `npx prisma init --datasource-provider postgresql`.
- سيتم إنشاء مجلد `prisma` وملف `.env` تلقائيًا. قم بتعديل متغير `DATABASE_URL` في ملف `.env` ليشير إلى قاعدة بيانات PostgreSQL.

### 5. تصميم مخطط قاعدة البيانات (Database Schema):
- افتح ملف `prisma/schema.prisma` وقم بتعريف النماذج (Models) التالية:
  - `User`: لبيانات المستخدمين (id, email, password, name, profilePicture, etc.).
  - `Message`: لرسائل الدردشة (id, content, senderId, receiverId, timestamp).
  - `Product`: للمنتجات التي يشاركها المستخدمون (id, name, description, imageUrl, externalLink, userId).
  - `Post`: للمنشورات الاجتماعية (id, content, userId, timestamp).

### 6. تطبيق المخطط على قاعدة البيانات:
- قم بتشغيل `npx prisma migrate dev --name init` لإنشاء الجداول في قاعدة البيانات بناءً على المخطط.

---

# 🔌 المرحلة الثانية: تطوير واجهة برمجة التطبيقات (API)

## الهدف:
بناء نقاط نهاية RESTful API لإدارة المستخدمين، الرسائل، المنشورات، والمنتجات.

## الخطوات:

### 1. تنظيم بنية الـ API:
- في مجلد `server/src`، قم بإنشاء مجلدات جديدة: `api`, `services`, `utils`.

### 2. إنشاء مسارات (Routes) للمستخدمين:
- في `server/src/api`، قم بإنشاء ملف `users.ts`.
- عرف مسارات لإنشاء مستخدم جديد (`POST /users`)، الحصول على بيانات مستخدم (`GET /users/:id`)، وتحديث بياناته (`PUT /users/:id`).

### 3. إنشاء مسارات للرسائل:
- في `server/src/api`، قم بإنشاء ملف `messages.ts`.
- عرف مسارات لإرسال رسالة (`POST /messages`) والحصول على سجل الدردشة بين مستخدمين (`GET /messages/:userId1/:userId2`).

### 4. إنشاء مسارات للمنشورات والمنتجات:
- قم بإنشاء ملفات `posts.ts` و `products.ts` في `server/src/api`.
- عرف مسارات CRUD (Create, Read, Update, Delete) أساسية لكل من المنشورات والمنتجات.

### 5. ربط المسارات بالخادم الرئيسي:
- في `server/src/index.ts`، قم باستيراد المسارات التي أنشأتها وربطها مع تطبيق Express.
- استخدم مسارًا أساسيًا للـ API مثل `/api/v1`.

---

# 🔐 المرحلة الثالثة: المصادقة والترخيص (Authentication & Authorization)

## الهدف:
تأمين الـ API والتطبيق باستخدام JWT (JSON Web Tokens).

## الخطوات:

### 1. تثبيت المكتبات اللازمة:
- قم بتثبيت `jsonwebtoken` و `bcryptjs` للتعامل مع التشفير والتوكنز.
- قم بتثبيت الأنواع الخاصة بها: `@types/jsonwebtoken`, `@types/bcryptjs`.

### 2. إنشاء مسارات المصادقة:
- في `server/src/api`، قم بإنشاء ملف `auth.ts`.
- عرف مسار لتسجيل الدخول (`POST /auth/login`) يقوم بالتحقق من بيانات المستخدم وإصدار JWT.
- عرف مسار للتسجيل (`POST /auth/register`) يقوم بإنشاء مستخدم جديد وتشفير كلمة المرور باستخدام `bcryptjs`.

### 3. إنشاء Middleware للتحقق من التوكن:
- في `server/src/api` أو `server/src/middleware`، قم بإنشاء middleware function تقوم بالتحقق من وجود وصحة الـ JWT في `Authorization` header.
- قم بحماية المسارات التي تتطلب مصادقة باستخدام هذا الـ middleware.

---

# 💬 المرحلة الرابعة: التواصل اللحظي (Real-time Communication)

## الهدف:
تحويل الدردشة من ردود محاكاة إلى تواصل لحظي حقيقي بين المستخدمين باستخدام WebSockets.

## الخطوات:

### 1. تثبيت مكتبة WebSockets:
- قم بتثبيت `socket.io` في الخادم.
- قم بتثبيت الأنواع الخاصة بها: `@types/socket.io`.

### 2. دمج WebSocket مع خادم Express:
- في `server/src/index.ts`، قم بإنشاء خادم WebSocket وربطه بخادم HTTP الموجود.

### 3. إدارة اتصالات WebSocket:
- عندما يقوم مستخدم بالاتصال، قم بتحديد هويته (بعد المصادقة) وتخزين الاتصال الخاص به.
- عند إرسال رسالة، ابحث عن اتصال المستلم وأرسل الرسالة إليه في الوقت الفعلي.
- قم بحفظ الرسالة في قاعدة البيانات عبر Prisma.

### 4. تحديث الواجهة الأمامية (Frontend):
- في الواجهة الأمامية، استخدم `socket.io-client` للاتصال بالخادم.
- استمع للأحداث الواردة (مثل `new_message`) وقم بتحديث واجهة المستخدم ديناميكيًا.

---

# 🔗 المرحلة الخامسة: ربط الواجهة الأمامية (Frontend) بالخلفية (Backend)

## الهدف:
تحويل الواجهة الأمامية من تطبيق ثابت يعتمد على بيانات وهمية إلى تطبيق ديناميكي يتفاعل مع الـ API الحقيقي.

## الخطوات:

### 1. إعداد عميل API:
- في الواجهة الأمامية (`/lib`)، قم بتثبيت `axios` لإجراء طلبات HTTP.
- أنشئ ملف `lib/api.ts` وقم بإعداد نسخة `axios` أساسية مع `baseURL` يشير إلى الخادم الخلفي (مثل `http://localhost:3001/api/v1`).

### 2. إدارة حالة المصادقة:
- أنشئ `AuthContext.tsx` في `contexts/` لإدارة حالة تسجيل دخول المستخدم (تخزين التوكن، بيانات المستخدم).
- قم بإنشاء صفحات/مكونات لتسجيل الدخول (`Login.tsx`) والتسجيل (`Register.tsx`).
- قم بتغليف التطبيق بـ `AuthProvider` في `app/layout.tsx`.

### 3. إعادة هيكلة صفحة الدردشة (`ChatPage.tsx`):
- احذف دالة `generateAIResponse` الوهمية.
- عند إرسال رسالة، قم باستدعاء نقطة نهاية `POST /messages` عبر `axios`.
- عند تحميل الصفحة، قم بجلب سجل الدردشة من `GET /messages/...`.
- قم بدمج عميل WebSocket لتلقي الرسائل الجديدة في الوقت الفعلي وتحديث الواجهة.

### 4. حماية المسارات:
- أنشئ مكون `ProtectedRoute.tsx` يقوم بالتحقق مما إذا كان المستخدم قد قام بتسجيل الدخول. إذا لم يكن كذلك، قم بإعادة توجيهه إلى صفحة تسجيل الدخول.
- قم بتغليف المسارات التي تتطلب مصادقة (مثل الدردشة، الملف الشخصي) بهذا المكون في `App.tsx`.

---

# 🎨 المرحلة السادسة: تطوير ميزات جديدة (اجتماعية وتجارية)

## الهدف:
إضافة صفحات ومكونات جديدة لتحويل التطبيق إلى منصة اجتماعية وتجارية.

## الخطوات:

### 1. إنشاء صفحة الملف الشخصي (`ProfilePage.tsx`):
- أنشئ صفحة جديدة يمكن للمستخدمين من خلالها عرض وتعديل معلوماتهم الشخصية (الاسم، الصورة، إلخ).
- يجب أن تعرض هذه الصفحة أيضًا المنشورات والمنتجات التي شاركها المستخدم.

### 2. إنشاء صفحة للمنشورات الاجتماعية (`FeedPage.tsx`):
- أنشئ صفحة جديدة تعرض موجزًا (feed) للمنشورات من المستخدمين الآخرين.
- أضف مكونًا لإنشاء منشور جديد (نص، صور).

### 3. إنشاء صفحة للمنتجات (`ProductsPage.tsx`):
- استبدل الصفحة المؤقتة الحالية بصفحة تعرض شبكة من المنتجات التي يشاركها المستخدمون.
- أضف نموذجًا (form) للسماح للمستخدمين بإضافة منتجات جديدة مع رابط خارجي (مثل Amazon, Noon, Shein, Alibaba, AliExpress).

### 4. تحديث شريط التنقل (`Navigation.tsx`):
- أضف روابط إلى الصفحات الجديدة (الملف الشخصي، الموجز، المنتجات).
- أظهر/أخفِ الروابط بناءً على حالة تسجيل دخول المستخدم.

---

# ✨ المرحلة السابعة: اللمسات الأخيرة والتحضير للنشر

## الهدف:
ضمان جودة الكود، إضافة معالجة شاملة للأخطاء، وإعداد المشروع للنشر.

## الخطوات:

### 1. إضافة معالجة الأخطاء الشاملة:
- في الخادم، قم بإنشاء middleware لمعالجة الأخطاء (error-handling middleware) لالتقاط جميع الأخطاء وإرسال استجابة JSON موحدة.
- في الواجهة الأمامية، استخدم مكون `ErrorBoundary` لتغليف المكونات الرئيسية ومنع تعطل التطبيق بالكامل بسبب خطأ في جزء منه.

### 2. إدارة متغيرات البيئة:
- تأكد من أن جميع المعلومات الحساسة (مثل `DATABASE_URL`, `JWT_SECRET`) يتم تحميلها من ملف `.env` ولا يتم تضمينها مباشرة في الكود.
- أنشئ ملف `.env.example` لتوثيق المتغيرات المطلوبة.

### 3. تحسين سكربتات `package.json`:
- في `package.json` الخاص بالواجهة الأمامية، أضف `proxy` لتوجيه طلبات الـ API إلى الخادم الخلفي أثناء التطوير.
- في `package.json` الخاص بالخادم، أضف سكربت `start` لتشغيل الخادم المترجم في بيئة الإنتاج، وسكربت `build` لترجمة كود TypeScript إلى JavaScript.

### 4. كتابة تعليمات النشر:
- قم بتحديث ملف `README.md` ليشمل تعليمات مفصلة حول كيفية بناء وتشغيل كل من الواجهة الأمامية والخلفية في بيئة الإنتاج.
- أضف معلومات عن متغيرات البيئة المطلوبة.
- أضف تعليمات للنشر على منصات مثل Vercel (للـ Frontend) و Railway أو Render (للـ Backend).

### 5. اختبار شامل:
- اختبر جميع نقاط نهاية الـ API.
- اختبر التكامل بين Frontend و Backend.
- اختبر WebSocket connections.
- اختبر المصادقة والترخيص.
- اختبر جميع الصفحات والمكونات.

---

# 🎯 ملاحظات مهمة

## ترتيب التنفيذ:
⚠️ **مهم جدًا:** اتبع المراحل بالترتيب لضمان بناء المشروع بشكل صحيح. كل مرحلة تعتمد على المراحل السابقة.

## الاختبار المستمر:
- بعد تنفيذ كل مرحلة، اختبر الميزات الجديدة.
- استخدم أدوات مثل Postman أو Thunder Client لاختبار نقاط نهاية الـ API.
- تحقق من أن قاعدة البيانات تعمل بشكل صحيح.
- تأكد من أن الواجهة تتواصل بشكل صحيح مع الخادم.

## إدارة الكود:
- استخدم Git لحفظ التقدم الذي تحرزه بعد كل مرحلة ناجحة.
- اكتب commit messages واضحة ووصفية.
- استخدم branches للعمل على ميزات جديدة.

## التوثيق:
- قم بتوثيق الكود المهم باستخدام comments.
- اكتب JSDoc comments للدوال المعقدة.
- حدث `README.md` بعد إضافة ميزات جديدة.

---

# ✅ Checklist التنفيذ

## المرحلة 1: ✅
- [x] مجلد server/ موجود
- [x] Express server يعمل
- [x] Prisma مُعد ومُهيأ
- [x] Database schema مُعرف

## المرحلة 2: ✅
- [x] API Routes للمستخدمين
- [x] API Routes للرسائل
- [x] API Routes للمنشورات
- [x] API Routes للمنتجات

## المرحلة 3: ✅
- [x] JWT Authentication
- [x] Auth Middleware
- [x] Routes محمية

## المرحلة 4: ✅
- [x] WebSocket مُعد
- [x] Socket.io integration
- [x] Real-time messaging

## المرحلة 5: ⏳
- [ ] Frontend API Client
- [ ] AuthContext
- [ ] تحديث صفحات Login/Register
- [ ] تحديث صفحة Chat

## المرحلة 6: ⏳
- [ ] صفحة Profile
- [ ] صفحة Feed
- [ ] تحديث صفحة Products

## المرحلة 7: ⏳
- [ ] Error Handling
- [ ] Environment Variables
- [ ] Production Scripts
- [ ] Documentation

---

# 🚀 الخلاصة

باتباع هذه المراحل، ستقوم بتحويل المشروع بشكل كامل إلى تطبيق ويب متكامل.

**تذكر:**
- اختبر كل ميزة بشكل جيد بعد تنفيذها
- احفظ تقدمك باستخدام Git
- راجع الكود قبل الانتقال للمرحلة التالية
- اسأل إذا كنت غير متأكد من شيء

**حظًا موفقًا في التطوير! 🎉**


