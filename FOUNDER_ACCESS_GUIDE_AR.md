# 🔐 دليل الوصول للمؤسس - Banda Chao

**التاريخ:** 2024-12-19  
**الحالة:** ✅ **دليل شامل للوصول للمؤسس**

---

## مرحباً! 👋

هذا الدليل يشرح **كيفية الوصول لمنصة Banda Chao كمؤسس**.

---

## 📋 معلومات الوصول

### 🌐 رابط الموقع

**Frontend (الموقع الرئيسي):**
- **الإنتاج:** `https://banda-chao.vercel.app`
- **أو:** `https://banda-chao-frontend.onrender.com`

**Backend (API):**
- **الإنتاج:** `https://banda-chao.onrender.com`

---

## 🔑 كيفية تسجيل الدخول

### الخطوة 1: اذهب إلى صفحة تسجيل الدخول

اذهب إلى:
```
https://banda-chao.vercel.app/login
```
أو
```
https://banda-chao.vercel.app/ar/login
```

### الخطوة 2: أدخل بيانات تسجيل الدخول

**خيار 1: تسجيل الدخول بالبريد الإلكتروني وكلمة المرور**
- أدخل **البريد الإلكتروني** الخاص بك
- أدخل **كلمة المرور**
- اضغط على "تسجيل الدخول"

**خيار 2: تسجيل الدخول باستخدام Google** (إن كان مفعلاً)
- اضغط على "تسجيل الدخول باستخدام Google"
- اختر حساب Google الخاص بك

---

## 🔍 البريد الإلكتروني المطلوب للوصول كمؤسس

### كيف يعمل النظام:

النظام يحدد إذا كنت **مؤسس** بناءً على:

1. **البريد الإلكتروني** الذي تستخدمه للتسجيل/تسجيل الدخول
2. **متغير البيئة `FOUNDER_EMAIL`** الموجود في Backend (Render)

### ⚠️ مهم جداً:

**يجب أن يكون البريد الإلكتروني الخاص بك مطابقاً تماماً لـ `FOUNDER_EMAIL` الموجود في Backend.**

**مثال:**
- إذا كان `FOUNDER_EMAIL=aljenaiditareq123@gmail.com` في Backend
- فيجب أن تستخدم نفس البريد الإلكتروني `aljenaiditareq123@gmail.com` للتسجيل/تسجيل الدخول

---

## 🔐 البريد الإلكتروني الحالي

**بناءً على الوثائق:**
- البريد الإلكتروني المذكور في التوثيق: `aljenaiditareq123@gmail.com`

**⚠️ لكن يجب التحقق:**
- هذا البريد قد يكون مثالياً في التوثيق
- يجب التحقق من `FOUNDER_EMAIL` الفعلي في Backend (Render)

---

## ✅ كيفية التحقق من البريد الإلكتروني

### 1. التحقق من Backend (Render)

**في Render Dashboard:**
1. اذهب إلى: https://dashboard.render.com
2. اختر خدمة Backend: `banda-chao-backend`
3. اذهب إلى: **Environment** tab
4. ابحث عن: `FOUNDER_EMAIL`
5. تحقق من القيمة

**إذا لم تجده:**
- يجب إضافته كمتغير بيئة جديد
- القيمة: بريدك الإلكتروني الكامل (مثال: `your-email@gmail.com`)

### 2. التحقق من قاعدة البيانات

**إذا كان لديك وصول لقاعدة البيانات:**
```sql
SELECT email, role FROM users WHERE email = 'your-email@gmail.com';
```

**يجب أن ترى:**
- `email`: بريدك الإلكتروني
- `role`: `FOUNDER`

---

## 🚨 حل المشاكل

### المشكلة 1: لا يمكنني الوصول لصفحة `/founder`

**الحل:**
1. تأكد من تسجيل الدخول أولاً
2. تحقق من أن البريد الإلكتروني مطابق لـ `FOUNDER_EMAIL` في Backend
3. تحقق من أن `role` في قاعدة البيانات هو `FOUNDER`
4. امسح الكاش والكوكيز وحاول مرة أخرى

**للتحقق:**
- افتح Developer Tools (F12)
- اذهب إلى Console
- تحقق من أي أخطاء
- اذهب إلى Network tab
- ابحث عن `/api/v1/users/me`
- تحقق من الـ `role` في الرد

### المشكلة 2: يتم توجيهي للصفحة الرئيسية بدلاً من `/founder`

**السبب:**
- أنت مسجل دخول، لكن `role` ليس `FOUNDER`

**الحل:**
1. **تحديث role في قاعدة البيانات:**
   ```sql
   UPDATE users SET role = 'FOUNDER' WHERE email = 'your-email@gmail.com';
   ```

2. **أو تحديث `FOUNDER_EMAIL` في Backend:**
   - اذهب إلى Render Dashboard
   - أضف/حدّث `FOUNDER_EMAIL` = بريدك الإلكتروني
   - أعد تشغيل Backend

3. **تسجيل خروج/دخول مرة أخرى:**
   - امسح الكاش والكوكيز
   - سجّل دخول مرة أخرى
   - يجب أن تحصل على `role: FOUNDER` تلقائياً

### المشكلة 3: "Invalid email or password"

**الحل:**
1. تأكد من صحة البريد الإلكتروني
2. تأكد من صحة كلمة المرور
3. إذا نسيت كلمة المرور:
   - يجب إعادة تعيينها من قاعدة البيانات
   - أو تسجيل حساب جديد بنفس البريد الإلكتروني

### المشكلة 4: البريد الإلكتروني تغير

**إذا كنت تستخدم بريد إلكتروني مختلف:**

1. **تحديث `FOUNDER_EMAIL` في Backend:**
   - اذهب إلى Render Dashboard
   - حدّث `FOUNDER_EMAIL` = البريد الإلكتروني الجديد
   - أعد تشغيل Backend

2. **تحديث role في قاعدة البيانات:**
   ```sql
   -- تحديث role للحساب الجديد
   UPDATE users SET role = 'FOUNDER' WHERE email = 'your-new-email@gmail.com';
   
   -- إزالة role من الحساب القديم (اختياري)
   UPDATE users SET role = 'USER' WHERE email = 'old-email@gmail.com';
   ```

3. **تسجيل خروج/دخول بالبريد الجديد:**
   - امسح الكاش والكوكيز
   - سجّل دخول بالبريد الإلكتروني الجديد

---

## 📝 خطوات الوصول الكاملة

### للمستخدم الجديد (ليس لديه حساب):

1. **اذهب إلى:** `https://banda-chao.vercel.app/register`
2. **سجّل حساب جديد:**
   - البريد الإلكتروني: **يجب أن يكون مطابقاً لـ `FOUNDER_EMAIL` في Backend**
   - كلمة المرور: اختر كلمة مرور قوية (6 أحرف على الأقل)
   - الاسم: اسمك
3. **اضغط "سجل الآن"**
4. **سجل دخول تلقائياً:**
   - إذا كان البريد مطابقاً لـ `FOUNDER_EMAIL`، ستحصل على `role: FOUNDER` تلقائياً
5. **اذهب إلى:** `/founder` أو `/founder/assistant`

### للمستخدم الحالي (لديه حساب):

1. **اذهب إلى:** `https://banda-chao.vercel.app/login`
2. **سجل دخول:**
   - البريد الإلكتروني: بريدك الإلكتروني
   - كلمة المرور: كلمة المرور الخاصة بك
3. **التحقق:**
   - افتح Developer Tools (F12)
   - اذهب إلى Network tab
   - ابحث عن `/api/v1/users/me`
   - تحقق من `role` - يجب أن يكون `"FOUNDER"`
4. **اذهب إلى:** `/founder` أو `/founder/assistant`

---

## 🔧 إعداد Backend (للمطور)

### إذا كنت بحاجة لتحديث `FOUNDER_EMAIL`:

**في Render Dashboard:**
1. اذهب إلى: https://dashboard.render.com
2. اختر: `banda-chao-backend` service
3. اذهب إلى: **Environment** tab
4. **أضف/حدّث متغير:**
   - Key: `FOUNDER_EMAIL`
   - Value: `your-email@gmail.com` (البريد الإلكتروني الكامل)
5. **احفظ**
6. Render سيعيد النشر تلقائياً

**ملاحظة مهمة:**
- تأكد من كتابة البريد الإلكتروني بشكل صحيح (بما في ذلك الأحرف الكبيرة/الصغيرة)
- النظام يقارن البريد بشكل case-insensitive، لكن من الأفضل كتابته بشكل صحيح

---

## 🌐 روابط الوصول

### صفحات المؤسس:
- **لوحة التحكم:** `/founder`
- **المساعد AI:** `/founder/assistant`
- **التحليلات:** `/founder/analytics`
- **الاعتدال:** `/founder/moderation`
- **المستندات:** `/founder/documents`
- **الجلسات:** `/founder/sessions`
- **الإعدادات:** `/founder/settings`

### صفحات عامة:
- **تسجيل الدخول:** `/login` أو `/ar/login`
- **التسجيل:** `/register` أو `/ar/register`
- **الصفحة الرئيسية:** `/` أو `/ar`

---

## ⚠️ ملاحظات مهمة

1. **البريد الإلكتروني حساس للحالة:**
   - النظام يقارن بشكل case-insensitive، لكن من الأفضل كتابته بشكل صحيح

2. **يجب أن يكون لديك حساب مسجل:**
   - يجب تسجيل حساب أولاً قبل الوصول كـ Founder
   - إذا لم يكن لديك حساب، سجّل حساب جديد

3. **تحديث Role تلقائياً:**
   - إذا كان البريد مطابقاً لـ `FOUNDER_EMAIL`، سيتم تحديث `role` تلقائياً عند تسجيل الدخول
   - إذا لم يحدث ذلك، يجب تحديث قاعدة البيانات يدوياً

4. **صفحة المؤسس عربية فقط:**
   - جميع صفحات `/founder` هي عربية فقط (RTL)
   - لا يوجد مبدّل اللغة في صفحات المؤسس

---

## 🆘 إذا لم تستطع الوصول بعد

### الخطوات التالية:

1. **تحقق من البريد الإلكتروني:**
   - تأكد من أن البريد الإلكتروني الذي تستخدمه للتسجيل/تسجيل الدخول مطابق تماماً لـ `FOUNDER_EMAIL` في Backend

2. **تحقق من كلمة المرور:**
   - تأكد من أن كلمة المرور صحيحة
   - إذا نسيتها، يجب إعادة تعيينها

3. **تحقق من قاعدة البيانات:**
   - تأكد من أن `role` في قاعدة البيانات هو `FOUNDER`
   - إذا لم يكن كذلك، قم بتحديثه:
     ```sql
     UPDATE users SET role = 'FOUNDER' WHERE email = 'your-email@gmail.com';
     ```

4. **تحقق من Backend:**
   - تأكد من أن `FOUNDER_EMAIL` موجود ومضبوط بشكل صحيح في Render Dashboard

5. **امسح الكاش والكوكيز:**
   - امسح كاش المتصفح والكوكيز
   - سجّل خروج/دخول مرة أخرى

6. **تحقق من Console:**
   - افتح Developer Tools (F12)
   - اذهب إلى Console tab
   - ابحث عن أي أخطاء
   - اذهب إلى Network tab
   - تحقق من ردود `/api/v1/users/me` و `/api/v1/auth/login`

---

## 📞 معلومات إضافية

### إذا كنت بحاجة للمساعدة:

**شارك معي:**
1. ما هو البريد الإلكتروني الذي تستخدمه؟
2. ما الذي يحدث عند محاولة الوصول لـ `/founder`؟
   - هل يتم توجيهك لصفحة تسجيل الدخول؟
   - هل يتم توجيهك للصفحة الرئيسية؟
   - هل ترى رسالة خطأ؟
3. ما هو رد `/api/v1/users/me`؟ (من Network tab)
   - ما هي قيمة `role`؟
4. هل `FOUNDER_EMAIL` موجود في Backend (Render)؟

---

**آمل أن يساعدك هذا الدليل! إذا كانت لديك أي أسئلة، دعني أعرف.** ✅

