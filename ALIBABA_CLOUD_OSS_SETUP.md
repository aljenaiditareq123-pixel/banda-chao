# 🚀 Alibaba Cloud OSS Setup Guide
**للانتقال إلى Alibaba Cloud OSS (مناسب للسوق الصيني)**

---

## 📋 المتغيرات البيئية المطلوبة (Environment Variables)

بعد شراء Alibaba Cloud OSS، أضف هذه المتغيرات في Render (Environment Variables):

### **المتغيرات الأساسية:**

| المتغير | الوصف | مثال |
|---------|-------|------|
| `ALIBABA_ACCESS_KEY_ID` | Access Key ID من Alibaba Cloud | `LTAI5t...` |
| `ALIBABA_ACCESS_KEY_SECRET` | Access Key Secret من Alibaba Cloud | `xxxxx...` |
| `ALIBABA_OSS_BUCKET` | اسم Bucket في OSS | `banda-chao-videos` |
| `ALIBABA_OSS_REGION` | المنطقة (Region) | `oss-cn-shanghai` |

### **متغير اختياري:**

| المتغير | الوصف | مثال |
|---------|-------|------|
| `ALIBABA_OSS_ENDPOINT` | Endpoint مخصص (إذا لم تستخدم Region) | `https://oss-cn-shanghai.aliyuncs.com` |

---

## 🎯 أولوية المزودين (Provider Priority)

الكود يختار المزود تلقائياً حسب الأولوية:

1. **Alibaba Cloud OSS** (الأولوية الأولى - للسوق الصيني)
2. **Google Cloud Storage** (Fallback - للسوق العالمي)

**ملاحظة:** إذا كان Alibaba OSS مُكوّن، سيستخدمه تلقائياً. إذا لم يكن، سيستخدم GCS.

---

## ✅ التحقق من التكوين

الكود يتحقق تلقائياً من التكوين:

- ✅ إذا كان Alibaba OSS مُكوّن → يستخدمه
- ✅ إذا لم يكن لكن GCS مُكوّن → يستخدم GCS
- ❌ إذا لم يكن أي منهما مُكوّن → **يرفض الرفع** مع رسالة "Storage service is under maintenance"

---

## 🔒 الأمان

**✅ التخزين المحلي معطل تماماً:**
- لا يمكن حفظ الفيديوهات محلياً
- يجب استخدام مزود تخزين سحابي (Alibaba OSS أو GCS)

---

## 📦 الحزمة المطلوبة

تم إضافة `ali-oss` package في `server/package.json`:
```json
"ali-oss": "^6.20.0"
```

**بعد إضافة المتغيرات:**
1. Restart Render service
2. الكود سيعمل تلقائياً بدون أي تعديلات إضافية!

---

## 🔧 الخطوات للحصول على Keys

1. إنشاء حساب Alibaba Cloud: https://www.alibabacloud.com
2. تفعيل OSS Service
3. إنشاء Bucket
4. الحصول على Access Key ID & Secret من IAM Console
5. إضافة المتغيرات في Render

---

**تاريخ الإنشاء:** 21 ديسمبر 2024  
**الحالة:** ✅ جاهز للاستخدام
